# 💬 دليل ربط Facebook Messenger بالتطبيق

## ✅ ما تم إنجازه:
- ✅ إنشاء Webhook API للماسنجر
- ✅ نظام رد تلقائي ذكي
- ✅ Quick Replies تفاعلية
- ✅ معالجة أنواع مختلفة من الرسائل

---

## 🚀 خطوات الربط الكامل:

### 1️⃣ إنشاء صفحة Facebook (إذا لم تكن موجودة)

1. اذهب: https://www.facebook.com/pages/create
2. أنشئ صفحة باسم: **Remo Store**
3. اختر Category: **Shopping & Retail**
4. املأ معلومات الصفحة

---

### 2️⃣ إنشاء Facebook App

1. اذهب: https://developers.facebook.com/apps
2. **Create App** → **Business** → Next
3. App Name: `Remo Store Messenger Bot`
4. **Create App**

---

### 3️⃣ إضافة Messenger Product

1. في App Dashboard
2. اضغط **Add Product**
3. اختار **Messenger** → **Set Up**

---

### 4️⃣ ربط الصفحة بالتطبيق

1. في Messenger Settings → **Access Tokens**
2. اختار صفحتك (Remo Store)
3. **Generate Token**
4. ⚠️ **انسخ الـ Token واحفظه!**

---

### 5️⃣ إعداد Webhook

1. في Messenger Settings → **Webhooks**
2. **Add Callback URL**:
   ```
   https://www.remostore.net/api/messenger/webhook
   ```
3. **Verify Token**: `remostore_messenger_2026` (أو اختار token خاص بك)
4. **Subscription Fields**: اختار:
   - ✅ messages
   - ✅ messaging_postbacks
   - ✅ message_deliveries
   - ✅ message_reads
5. **Verify and Save**

---

### 6️⃣ إضافة المتغيرات في .env

أضف في ملف `.env`:

```env
# Messenger Bot Configuration
MESSENGER_VERIFY_TOKEN=remostore_messenger_2026
MESSENGER_PAGE_ACCESS_TOKEN=your_page_access_token_here
```

⚠️ استبدل `your_page_access_token_here` بالـ token اللي حصلت عليه!

---

### 7️⃣ نشر التطبيق على الإنترنت

الـ Webhook يجب أن يكون متاح على الإنترنت (https):

```bash
# على Vercel
vercel --prod

# أو على Netlify
netlify deploy --prod
```

⚠️ **مهم:** يجب أن يكون الموقع على `https` (مش `http`)

---

## 🧪 اختبار الرد التلقائي

### 1. من صفحة Facebook:
1. اذهب لصفحتك على Facebook
2. اضغط **Send Message**
3. اكتب: `مرحبا`
4. يجب أن يرد البوت تلقائياً! ✅

### 2. أمثلة على الرسائل:

```
المستخدم: السلام عليكم
البوت: مرحباً بك في ريمو ستور! 👋
       كيف يمكنني مساعدتك اليوم؟
       🛍️ تسوق المنتجات...

المستخدم: عاوز اشتري ملابس
البوت: 🛍️ تسوق أحدث منتجاتنا!
       ✨ ملابس عصرية...

المستخدم: سعر كام؟
البوت: 💰 الأسعار والعروض:
       🔥 خصومات تصل لـ 50%...
```

---

## 🎯 الردود الذكية المتوفرة:

البوت يرد تلقائياً على:

### ترحيب:
- `السلام عليكم` / `مرحبا` / `hi` / `hello`
- → رد ترحيبي مع Quick Replies

### المنتجات:
- `منتجات` / `ملابس` / `product`
- → عرض المنتجات والموقع

### الطلبات:
- `طلب` / `شحن` / `توصيل` / `order`
- → معلومات الشحن والتوصيل

### الأسعار:
- `سعر` / `كام` / `price`
- → معلومات الأسعار والعروض

### التواصل:
- `تواصل` / `رقم` / `contact`
- → بيانات التواصل

### المساعدة:
- `مساعدة` / `help`
- → قائمة بالأوامر المتاحة

### التطبيق:
- `تطبيق` / `app` / `download`
- → رابط تحميل التطبيق

---

## ⚙️ تخصيص الردود

في ملف `src/app/api/messenger/webhook/route.ts`:

```typescript
// مثال: إضافة رد جديد
else if (messageText.includes('عروض') || messageText.includes('offers')) {
  replyText = `🔥 عروض اليوم الحصرية!\n\n...`
}
```

---

## 📊 مراقبة الرسائل

### في Console:
```bash
# شغل السيرفر وشوف اللوجز
npm run dev

# هتشوف:
📩 رسالة جديدة من Messenger: ...
📨 رسالة من 123456789: مرحبا
✅ تم إرسال الرد بنجاح
```

---

## 🔐 الأمان:

1. **Verify Token:**
   - اختار token قوي
   - لا تشاركه مع أحد
   - غيّره لو تسرب

2. **Page Access Token:**
   - احفظه في `.env`
   - لا ترفعه على Git
   - له صلاحية غير محدودة

3. **HTTPS مطلوب:**
   - Facebook يطلب HTTPS
   - استخدم Vercel/Netlify

---

## 🚨 حل المشاكل:

### Webhook Verification فشل:
- تأكد من `VERIFY_TOKEN` صحيح في `.env`
- تأكد من الـ URL صحيح
- تأكد من الموقع شغال (https)

### البوت لا يرد:
- تأكد من `PAGE_ACCESS_TOKEN` صحيح
- شوف console logs للأخطاء
- تأكد من subscription fields مفعلة

### Token انتهت صلاحيته:
- احصل على token جديد من Facebook
- حدث `.env`
- أعد تشغيل السيرفر

---

## 📱 ربط مع التطبيق:

### في التطبيق، يمكنك:

1. **زر Messenger:**
```tsx
<a href="https://m.me/your_page_id" 
   className="btn-messenger">
  💬 تواصل عبر Messenger
</a>
```

2. **Facebook Customer Chat Plugin:**
```html
<div class="fb-customerchat"
     attribution="setup_tool"
     page_id="your_page_id">
</div>
```

---

## 🎉 النتيجة:

بعد إتمام الخطوات:
- ✅ ماسنجر مربوط بالموقع
- ✅ رد تلقائي ذكي يعمل 24/7
- ✅ تجربة تفاعلية للعملاء
- ✅ توفير وقت فريق خدمة العملاء

---

## 📚 موارد إضافية:

- [Messenger Platform Docs](https://developers.facebook.com/docs/messenger-platform)
- [Webhooks Reference](https://developers.facebook.com/docs/messenger-platform/webhook)
- [Send API Reference](https://developers.facebook.com/docs/messenger-platform/reference/send-api)

---

## ⏭️ الخطوات التالية:

1. **إنشاء Facebook App**
2. **الحصول على Page Access Token**
3. **إضافة Token في `.env`**
4. **ربط Webhook**
5. **اختبار الرد التلقائي**

---

**جاهز للبدء!** 🚀

ابدأ بإنشاء Facebook App من:
https://developers.facebook.com/apps
