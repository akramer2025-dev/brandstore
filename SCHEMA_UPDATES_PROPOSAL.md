# 📋 التحديثات المطلوبة على Schema

## ⚠️ قبل التطبيق - مراجعة مطلوبة!

### 1️⃣ إضافة Enum جديد لطريقة التوصيل

```prisma
enum DeliveryMethod {
  HOME_DELIVERY    // توصيل للمنزل
  STORE_PICKUP     // استلام من المتجر
}
```

---

### 2️⃣ إضافة Model جديد لمناطق التوصيل

```prisma
model DeliveryZone {
  id            String   @id @default(cuid())
  governorate   String   @unique // اسم المحافظة
  deliveryFee   Float    // رسوم التوصيل
  minOrderValue Float    @default(0) // الحد الأدنى لقيمة الطلب
  isActive      Boolean  @default(true)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@map("delivery_zones")
}
```

**أمثلة البيانات:**
- القاهرة: 125 ج.م
- الإسكندرية: 200 ج.م
- الجيزة: 150 ج.م

---

### 3️⃣ تحديث Order Model (إضافة حقول فقط - بدون حذف)

```prisma
model Order {
  // ... جميع الحقول الموجودة تبقى كما هي
  
  // ✨ حقول جديدة فقط:
  deliveryMethod   DeliveryMethod @default(HOME_DELIVERY)
  pickupLocation   String?        // عنوان المتجر للاستلام
  governorate      String?        // المحافظة (للتوصيل المنزلي)
  downPayment      Float?         // الدفعة المقدمة
  remainingAmount  Float?         // المبلغ المتبقي
  
  // الحقول الموجودة لن تتأثر!
}
```

---

### 4️⃣ إضافة Model لإعدادات النظام

```prisma
model SystemSettings {
  id                    String   @id @default(cuid())
  key                   String   @unique
  value                 String
  description           String?
  updatedAt             DateTime @updatedAt

  @@map("system_settings")
}
```

**إعدادات افتراضية:**
- `min_down_payment_percent`: 30 (نسبة الدفع المقدم للاستلام من المتجر)
- `default_delivery_fee`: 125 (رسوم توصيل افتراضية)

---

## ✅ الأمان:

1. **لن يتم حذف أي بيانات موجودة**
2. **جميع الحقول الجديدة optional** (يمكن أن تكون null)
3. **الـ default values موجودة** لتجنب المشاكل
4. **الطلبات القديمة ستستمر في العمل** بدون مشاكل

---

## 🚀 الأمر المطلوب تنفيذه:

```bash
npx prisma db push
```

**هل توافق على تطبيق هذه التحديثات؟**
- ✅ نعم - ابدأ التطبيق
- ❌ لا - أريد مراجعة أو تعديل

