# دليل حل المشاكل الثلاثة

## ✅ 1. مشكلة إضافة الشريك - تم الحل

### المشكلة:
عند إضافة شريك جديد وملء البيانات والضغط على "حفظ الشريك"، يظهر خطأ.

### الحل المطبق:
- ✅ إضافة التحقق من كلمة المرور في API
- ✅ إصلاح متغير `password` الذي كان يتعارض مع المتغير المحلي
- ✅ إضافة رسائل خطأ واضحة تخبرك بالمشكلة بالضبط

### الملفات المعدلة:
- `src/app/api/admin/partners/route.ts`

### كيفية الاستخدام:
1. اذهب إلى `/admin/partners`
2. اضغط "إضافة شريك"
3. املأ البيانات
4. إذا اخترت "إنشاء حساب VENDOR"، **يجب** إدخال كلمة المرور
5. اضغط "حفظ الشريك"

---

## ✅ 2. تسجيل الدخول بـ Google للعملاء - تم الإضافة

### الملفات المعدلة:
- `src/lib/auth.ts` - إضافة Google Provider
- `src/app/auth/login/page.tsx` - إضافة زر Google

### المتطلبات:
تحتاج للحصول على Google OAuth Credentials:

1. **اذهب إلى**: https://console.cloud.google.com
2. **أنشئ مشروع جديد** أو اختر مشروع موجود
3. **اذهب إلى**: APIs & Services → Credentials
4. **اضغط**: Create Credentials → OAuth 2.0 Client ID
5. **اختر**: Web application
6. **Authorized redirect URIs**:
   ```
   http://localhost:3000/api/auth/callback/google
   https://yourdomain.com/api/auth/callback/google
   ```
7. **انسخ**:
   - Client ID
   - Client Secret
8. **ضعهم في `.env`**:
   ```env
   GOOGLE_CLIENT_ID=your-client-id-here
   GOOGLE_CLIENT_SECRET=your-client-secret-here
   ```

### كيفية الاستخدام:
1. املأ `GOOGLE_CLIENT_ID` و `GOOGLE_CLIENT_SECRET` في `.env`
2. أعد تشغيل `npm run dev`
3. اذهب إلى `/auth/login`
4. اضغط على زر "تسجيل الدخول بواسطة Google"
5. سيتم إنشاء حساب جديد تلقائياً بدور `CUSTOMER`

---

## ⚠️ 3. مشكلة رفع الصور - تحتاج فحص

### نظام رفع الصور يعمل بشكل صحيح

الكود محسّن ويعمل، لكن هناك شروط:

### الشروط المطلوبة:
1. ✅ يجب تسجيل الدخول كـ **VENDOR** أو **ADMIN**
2. ✅ حجم الصورة أقل من **5 ميجابايت**
3. ✅ نوع الصورة: **JPEG, PNG, WebP فقط**

### كيفية فحص المشكلة:

#### 1. افتح Console في المتصفح (F12)
اذهب إلى Console Tab وشاهد رسائل الخطأ

#### 2. تحقق من تسجيل الدخول:
```bash
npx tsx test-upload-debug.ts
```

سيعرض لك:
- عدد حسابات الـ VENDOR
- قائمة بأسماء الـ VENDOR

#### 3. تحقق من حجم الصورة:
- في Windows: انقر يمين على الصورة → Properties → Size
- يجب أن تكون أقل من 5 MB (5,242,880 bytes)

#### 4. الأخطاء الشائعة:

| الخطأ | السبب | الحل |
|-------|-------|------|
| `Unauthorized` | لم تسجل دخول أو ليس VENDOR/ADMIN | سجل دخول كشريك أو مدير |
| `Invalid file type` | نوع الملف غير مدعوم | استخدم JPEG, PNG, أو WebP |
| `File exceeds 5MB` | الملف كبير جداً | قلّل حجم الصورة |
| `Failed to upload` | خطأ في السيرفر | افتح Console لرؤية التفاصيل |

### رسائل الخطأ المحسنة:

الآن عند رفع صورة، ستحصل على رسالة واضحة:

```
❌ حجم الملف كبير جداً: photo.jpg

الحجم: 8.5 MB
الحد الأقصى: 5 MB
```

أو:

```
❌ نوع الملف غير مسموح: image.gif

يُسمح فقط بـ: JPEG, PNG, WebP
```

---

## 📝 ملاحظات مهمة:

### للمشكلة الأولى (إضافة الشريك):
- تم الحل كاملاً ✅
- جرب إضافة شريك الآن وستعمل

### للمشكلة الثانية (Google OAuth):
- الكود جاهز ✅
- تحتاج فقط Google Credentials
- اتبع الخطوات أعلاه

### للمشكلة الثالثة (رفع الصور):
- النظام يعمل ✅
- لكن **تأكد من**:
  1. أنك مسجل دخول كـ VENDOR أو ADMIN
  2. حجم الصورة < 5 MB
  3. نوع الصورة: JPEG/PNG/WebP

---

## 🧪 للاختبار:

```bash
# 1. تشغيل المشروع
npm run dev

# 2. فحص الـ VENDOR accounts
npx tsx test-upload-debug.ts

# 3. تسجيل دخول كشريك
# اذهب إلى: http://localhost:3000/auth/login
# استخدم بريد الشريك وكلمة المرور

# 4. جرب رفع صورة
# اذهب إلى: /vendor/products/new
# جرب رفع صورة
```

---

## 🚀 الخطوات التالية:

1. **جرب إضافة شريك** - يجب أن تعمل الآن
2. **احصل على Google Credentials** - إذا أردت تسجيل دخول Google
3. **جرب رفع صورة** - تأكد من تسجيل دخولك كـ VENDOR
4. **افتح Console** - لرؤية أي أخطاء تفصيلية

