# 📐 مقاسات وأبعاد صفحة إدارة الشركاء
**الصفحة:** https://www.remostore.net/admin/partners

---

## 🖥️ **نموذج إضافة الشريك (Dialog)**

### العرض (Width):
```css
/* موبايل (< 640px) */
w-[calc(100vw-16px)]      /* = عرض الشاشة - 16 بكسل */
                          /* مثال: على موبايل 375px = 359px */

/* تابلت (≥ 640px) */
sm:w-[calc(100vw-32px)]   /* = عرض الشاشة - 32 بكسل */
                          /* مثال: على تابلت 768px = 736px */

/* حد أقصى للعرض */
sm:max-w-md               /* = 448px (28rem) على التابلت */
md:max-w-lg               /* = 512px (32rem) على الشاشات المتوسطة */
```

### الارتفاع (Height):
```css
max-h-[90vh]              /* = 90% من ارتفاع الشاشة */
                          /* مثال: شاشة 800px = 720px */
overflow-y-auto           /* سكرول عمودي عند الحاجة */
```

### Padding (المسافات الداخلية):
```css
/* موبايل */
p-4                       /* = 16px من كل الجوانب */

/* تابلت وأكبر */
sm:p-6                    /* = 24px من كل الجوانب */
```

---

## 📋 **DialogHeader (رأس النموذج)**

```css
pb-2                      /* padding-bottom = 8px */
```

### العنوان (Title):
```css
text-lg                   /* = 18px على الموبايل */
sm:text-xl                /* = 20px على التابلت */
```

### الوصف (Description):
```css
text-sm                   /* = 14px */
```

---

## 📝 **حقول الإدخال (Inputs)**

### Labels:
```css
text-sm                   /* font-size = 14px */
```

### Input Fields:
```css
h-9                       /* height = 36px */
```

### Textarea:
```css
min-h-[50px]              /* minimum height = 50px */
rows={2}                  /* صفين فقط */
```

### Select Dropdown:
```css
h-9                       /* height = 36px */
text-sm                   /* font-size = 14px */
```

---

## 📦 **Checkboxes & Labels**

### Checkbox Containers:
```css
p-2                       /* padding = 8px */
text-sm                   /* font-size = 14px */
```

### Warning Text:
```css
text-[11px]               /* = 11px */
```

---

## 🔘 **الأزرار (Buttons)**

### حجم الأزرار:
```css
h-9                       /* height = 36px */
text-sm                   /* font-size = 14px */
```

### Container الأزرار:
```css
gap-2                     /* مسافة بين الأزرار = 8px */
pt-3                      /* padding-top = 12px */
pb-1                      /* padding-bottom = 4px */
sticky bottom-0           /* ثابتة في الأسفل */
bg-gray-900               /* خلفية داكنة */
```

---

## 📏 **المسافات (Spacing)**

### بين العناصر الرئيسية:
```css
space-y-3                 /* = 12px بين العناصر */
mt-2                      /* margin-top = 8px */
```

### بين الحقول:
```css
space-y-2.5               /* = 10px بين الحقول */
```

### بين الـ Checkboxes:
```css
space-y-1.5               /* = 6px */
```

---

## 🎨 **Scrollbar (شريط التمرير)**

```css
scrollbar-thin            /* شريط رفيع */
scrollbar-thumb-purple-500  /* لون أرجواني */
scrollbar-track-gray-800    /* خلفية رمادية داكنة */
```

---

## 📱 **Breakpoints (نقاط التحول)**

```css
sm: 640px                 /* تابلت صغير */
md: 768px                 /* تابلت كبير */
lg: 1024px                /* لابتوب */
xl: 1280px                /* شاشة كبيرة */
```

---

## 🔢 **أمثلة بالأرقام:**

### على موبايل iPhone 12 (390 × 844):
- **عرض النموذج:** 374px (390 - 16)
- **ارتفاع النموذج:** 759px (90% من 844)
- **Padding:** 16px
- **ارتفاع الحقل:** 36px

### على تابلت iPad (768 × 1024):
- **عرض النموذج:** 448px (max-w-md)
- **ارتفاع النموذج:** 921px (90% من 1024)
- **Padding:** 24px
- **ارتفاع الحقل:** 36px

### على شاشة Desktop 13" (1440 × 900):
- **عرض النموذج:** 512px (md:max-w-lg)
- **ارتفاع النموذج:** 810px (90% من 900)
- **Padding:** 24px
- **ارتفاع الحقل:** 36px

---

## 📊 **ملخص سريع:**

```
النموذج:
├─ العرض: 359-512px (حسب الشاشة)
├─ الارتفاع: max 90% من الشاشة
├─ Padding: 16-24px
│
الحقول:
├─ ارتفاع: 36px
├─ Labels: 14px
├─ Inputs text: 14px
│
المسافات:
├─ بين الحقول: 10px
├─ بين الأزرار: 8px
│
الأزرار:
├─ ارتفاع: 36px
└─ حجم النص: 14px
```

---

**هذه الأبعاد الحالية للنموذج بعد التحديث الأخير** ✅
