# 📦 دليل نقل المنتجات بين البائعين

## نظرة عامة

هذا الدليل يشرح كيفية **نقل منتجات من بائع (Vendor) لبائع آخر بطريقة آمنة** دون التأثير على البيانات الموجودة.

---

## 🔍 الفرق بين Vendor و PartnerCapital

### 📌 Vendor (البائع/التاجر)
- كيان واحد يمثل المحل أو الأعمال التجارية
- **المنتجات تنتمي للـ Vendor** (عبر `vendorId`)
- كل Vendor له حساب مستخدم واحد
- له رأس مال خاص به

### 📌 PartnerCapital (الشريك في رأس المال)
- شريك يساهم في رأس مال Vendor معين
- **ليس له منتجات خاصة** - المنتجات تنتمي للـ Vendor ككل
- الشركاء يتشاركون في الأرباح فقط
- Vendor واحد يمكن أن يكون له عدة شركاء

### 🎯 مثال للتوضيح:
```
Vendor A (محل الملابس) ← له 50 منتج
  ├─ PartnerCapital 1: أحمد (50% من رأس المال)
  ├─ PartnerCapital 2: محمد (30% من رأس المال)
  └─ PartnerCapital 3: علي (20% من رأس المال)

المنتجات تنتمي للـ Vendor A ككل، وليس لشريك معين!
```

---

## 🚀 خطوات نقل المنتجات

### الخطوة 1️⃣: عرض جميع البائعين

أولاً، شوف كل البائعين الموجودين في النظام:

```bash
npx tsx list-all-vendors.ts
```

**النتيجة:**
- قائمة بجميع البائعين
- Vendor ID لكل بائع
- عدد المنتجات لكل بائع
- حالة كل بائع (نشط/موقوف)

انسخ الـ **Vendor ID** للبائع القديم والجديد.

---

### الخطوة 2️⃣: عرض منتجات البائع القديم

شوف المنتجات الموجودة عند البائع القديم:

1. افتح ملف `list-vendor-products.ts`
2. غيّر `VENDOR_ID` بـ ID البائع اللي عاوز تشوف منتجاته:

```typescript
const VENDOR_ID = 'clz123abc...'; // ضع ID البائع القديم هنا
```

3. شغّل السكريبت:

```bash
npx tsx list-vendor-products.ts
```

**النتيجة:**
- قائمة مفصلة بكل منتجات البائع
- IDs المنتجات جاهزة للنسخ
- إحصائيات عن كل منتج (الطلبات، التقييمات، المخزون)

---

### الخطوة 3️⃣: نقل المنتجات

الآن جاهز تنقل المنتجات!

1. افتح ملف `transfer-products-between-vendors.ts`
2. املأ البيانات المطلوبة:

```typescript
// 👇 ضع ID البائع القديم (اللي هتنقل منه المنتجات)
const FROM_VENDOR_ID = 'clz123abc...'; 

// 👇 ضع ID البائع الجديد (اللي هتنقل له المنتجات)
const TO_VENDOR_ID = 'clz456def...'; 

// 👇 ضع IDs المنتجات اللي عاوز تنقلها
const PRODUCT_IDS = [
  'prod_123abc...',
  'prod_456def...',
  'prod_789ghi...',
];
```

3. شغّل السكريبت:

```bash
npx tsx transfer-products-between-vendors.ts
```

---

## ✅ ماذا يفعل السكريبت؟

السكريبت يقوم بـ:

### 1. التحقق من البيانات
- ✅ التأكد من وجود البائع القديم
- ✅ التأكد من وجود البائع الجديد
- ✅ التأكد من أن البائع الجديد نشط
- ✅ التأكد من أن المنتجات تنتمي للبائع القديم

### 2. عرض المعلومات
- 📊 معلومات عن كل منتج
- 📦 عدد الطلبات المرتبطة
- ⭐ عدد التقييمات
- 🛒 المنتجات في السلة
- ❤️ المنتجات في المفضلة

### 3. النقل الآمن (Transaction)
- 🔄 تحديث `vendorId` في جدول المنتجات
- 📊 تحديث إحصائيات البائع القديم
- 📊 تحديث إحصائيات البائع الجديد
- 📦 نقل عناصر المخزون (Inventory) المرتبطة

### 4. التحقق النهائي
- ✅ التأكد من نجاح النقل
- 📋 عرض ملخص العملية

---

## ⚠️ تحذيرات مهمة

### 1. المنتجات المرتبطة بطلبات
- إذا كان المنتج مرتبط بطلبات موجودة، سيتم النقل لكن:
  - ✅ الطلبات القديمة تظل كما هي
  - ✅ الطلبات الجديدة تُنسب للبائع الجديد
  - ⚠️ **لن تتأثر الطلبات التاريخية**

### 2. العموالت والأرباح
- العموالت السابقة تظل للبائع القديم
- العموالت الجديدة تُحسب للبائع الجديد

### 3. النسخ الاحتياطي
- يُفضل عمل **نسخة احتياطية** من قاعدة البيانات قبل النقل:

```bash
npx tsx backup-all-data.ts
```

---

## 🛡️ الحماية من الأخطاء

السكريبت آمن تماماً لأنه:

1. ✅ يستخدم **Transaction** - إذا حصل خطأ، كل شيء يرجع كما كان
2. ✅ يتحقق من صحة البيانات قبل التنفيذ
3. ✅ يعرض تحذيرات قبل نقل منتجات مرتبطة بطلبات
4. ✅ لا يحذف أي بيانات - فقط يحدث vendorId

---

## 📊 مثال عملي كامل

### السيناريو
- عندك بائع قديم (أحمد) - له 10 منتجات
- عاوز تنقل 3 منتجات لبائع جديد (محمد)

### الخطوات

#### 1. شوف كل البائعين
```bash
npx tsx list-all-vendors.ts
```

**النتيجة:**
```
1. ✅ محل أحمد
   🆔 Vendor ID: clz123abc
   📦 المنتجات: 10

2. ✅ محل محمد
   🆔 Vendor ID: clz456def
   📦 المنتجات: 5
```

#### 2. شوف منتجات أحمد
عدّل `list-vendor-products.ts`:
```typescript
const VENDOR_ID = 'clz123abc';
```

شغّل:
```bash
npx tsx list-vendor-products.ts
```

**النتيجة:**
```
💡 نسخ IDs المنتجات:
const PRODUCT_IDS = [
  'prod_123', // فستان أحمر
  'prod_456', // قميص أزرق
  'prod_789', // بنطلون أسود
  ...
];
```

#### 3. نقل المنتجات
عدّل `transfer-products-between-vendors.ts`:
```typescript
const FROM_VENDOR_ID = 'clz123abc'; // أحمد
const TO_VENDOR_ID = 'clz456def'; // محمد
const PRODUCT_IDS = [
  'prod_123', // فستان أحمر
  'prod_456', // قميص أزرق
  'prod_789', // بنطلون أسود
];
```

شغّل:
```bash
npx tsx transfer-products-between-vendors.ts
```

**النتيجة:**
```
✅ تم نقل المنتجات بنجاح!

📊 ملخص العملية:
   - عدد المنتجات المنقولة: 3
   - من: محل أحمد
   - إلى: محل محمد

🔍 التحقق النهائي من المنتجات المنقولة:
1. فستان أحمر - ✅ تم النقل
2. قميص أزرق - ✅ تم النقل
3. بنطلون أسود - ✅ تم النقل
```

---

## ❓ أسئلة شائعة

### ❓ هل يمكن نقل منتجات مرتبطة بطلبات؟
✅ **نعم** - الطلبات القديمة تظل كما هي، فقط الطلبات الجديدة تُنسب للبائع الجديد.

### ❓ ماذا يحدث للتقييمات؟
✅ التقييمات تظل مرتبطة بالمنتج - لا تتأثر بالنقل.

### ❓ هل يمكن إلغاء النقل؟
⚠️ بعد التنفيذ، لا يمكن الرجوع تلقائياً. لكن يمكنك نقل المنتجات مرة أخرى للبائع القديم.

### ❓ ماذا لو حصل خطأ أثناء النقل؟
✅ لن يحدث شيء! السكريبت يستخدم Transaction - إذا فشل، كل شيء يرجع كما كان.

### ❓ هل يؤثر على المخزون (Inventory)؟
✅ لا - المخزون يُنقل تلقائياً مع المنتجات.

---

## 🔧 استكشاف الأخطاء

### ❌ البائع غير موجود
تأكد من:
- نسخ Vendor ID صحيح
- عدم وجود مسافات زيادة
- استخدام الـ ID الكامل

### ❌ لم يتم العثور على منتجات
تأكد من:
- المنتجات فعلاً تنتمي للبائع القديم
- Product IDs صحيحة
- عدم نقل نفس المنتجات مرتين

### ❌ البائع الجديد غير نشط
- تأكد أن `isActive = true` للبائع الجديد
- أو فعّل البائع من لوحة الإدارة

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. راجع الرسائل في Terminal
2. تأكد من صحة البيانات المدخلة
3. اعمل نسخة احتياطية أولاً

---

## ✅ الخلاصة

الآن عندك 3 سكريبتات قوية:

1. **`list-all-vendors.ts`** - عرض كل البائعين
2. **`list-vendor-products.ts`** - عرض منتجات بائع معين
3. **`transfer-products-between-vendors.ts`** - نقل المنتجات بأمان

استخدمهم بالترتيب، واتبع الخطوات المذكورة، وكل شيء هيشتغل تمام! ✅
