# 🏦 نظام التقسيط المتطور - دليل كامل

تاريخ التحديث: فبراير 2026

---

## 📋 نظرة عامة

نظام تقسيط متطور مع **اتفاقية قانونية** تتطلب 3 خطوات إلزامية:
1. ✅ رفع صورة البطاقة الشخصية
2. ✅ التوقيع الإلكتروني على الشاشة
3. ✅ رفع صورة شخصية (سيلفي)

---

## 🎯 المميزات الرئيسية

### ✨ للعملاء:
- تقسيط على 6، 12، أو 24 شهر
- بدون فوائد (حالياً)
- مقدم 30% من قيمة الطلب
- موافقة سريعة خلال دقائق
- واجهة سهلة وبسيطة

### 🔒 الحماية القانونية:
- اتفاقية مُلزمة قانونياً
- تحذير واضح من العقوبات
- توثيق كامل بالمستندات
- حفظ IP و User Agent
- سجل كامل للمراجعة

### 👨‍💼 للإدارة:
- مراجعة الاتفاقيات
- التحقق من المستندات
- قبول أو رفض الطلبات
- تقارير تفصيلية

---

## 🚀 كيفية الاستخدام

### 📱 من جانب العميل:

#### 1. اختيار التقسيط
```
1. الذهاب لصفحة Checkout
2. اختيار "🏦 الدفع بالتقسيط"
3. الضغط على "ابدأ اتفاقية التقسيط"
```

#### 2. صفحة الاتفاقية
```
URL: /installment-agreement?totalAmount=X&downPayment=Y&installments=Z
```

**الخطوات:**

##### الخطوة 1️⃣: رفع البطاقة الشخصية
- رفع صورة واضحة للبطاقة الشخصية
- يجب أن تكون جميع البيانات مقروءة
- حجم الصورة أقل من 5MB
- صيغة الصورة: JPG, PNG, WebP

##### الخطوة 2️⃣: التوقيع الإلكتروني
- التوقيع على لوحة الرسم
- استخدام الماوس أو الإصبع (موبايل)
- يتم حفظ التوقيع كـ base64
- إمكانية مسح وإعادة التوقيع

##### الخطوة 3️⃣: صورة شخصية + الموافقة
- رفع صورة شخصية (سيلفي)
- إدخال الاسم الكامل (اختياري)
- إدخال الرقم القومي (اختياري)
- **الموافقة على الشروط** (إجباري)

#### 3. إرسال الاتفاقية
```
- تحميل الصور إلى Cloudinary
- إنشاء سجل في قاعدة البيانات
- حالة الاتفاقية: DOCUMENTS_COMPLETE
- العودة لصفحة Checkout
```

---

## 🗄️ قاعدة البيانات

### جدول InstallmentAgreement

```prisma
model InstallmentAgreement {
  id                    String   @id @default(cuid())
  userId                String
  orderId               String?  @unique
  
  // معلومات الاتفاقية
  agreementNumber       String   @unique // مثل: AGR-ABC123-DEF456
  status                InstallmentAgreementStatus @default(PENDING)
  
  // المستندات (الخطوات الثلاثة)
  nationalIdImage       String?  // Cloudinary URL
  signature             String?  // base64 أو Cloudinary URL
  selfieImage           String?  // Cloudinary URL
  
  // بيانات العميل
  fullName              String?
  nationalId            String?  // الرقم القومي
  address               String?
  
  // تفاصيل التقسيط
  totalAmount           Float    // المبلغ الإجمالي
  downPayment           Float    // المقدم
  numberOfInstallments  Int      // عدد الأقساط
  monthlyInstallment    Float    // القسط الشهري
  interestRate          Float    @default(0)
  
  // حالة الموافقة
  acceptedTerms         Boolean  @default(false)
  acceptedAt            DateTime?
  
  // معلومات إضافية
  ip                    String?
  userAgent             String?
  
  // التدقيق
  verifiedBy            String?  // admin/developer
  verifiedAt            DateTime?
  verificationNotes     String?
  rejectionReason       String?
  
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt
}
```

### حالات الاتفاقية (Status)

```typescript
enum InstallmentAgreementStatus {
  PENDING              // في انتظار استكمال المستندات
  DOCUMENTS_COMPLETE   // المستندات مكتملة - جاهز للمراجعة
  UNDER_REVIEW         // تحت المراجعة من الإدارة
  APPROVED             // تمت الموافقة
  REJECTED             // مرفوض
  EXPIRED              // منتهي الصلاحية
}
```

---

## 🔧 الملفات والمكونات

### Frontend

#### 1. `SignaturePad.tsx`
```typescript
// كومبوننت لوحة التوقيع الإلكتروني
- Canvas للرسم
- دعم Mouse و Touch
- حفظ التوقيع كـ base64
- زر مسح التوقيع
```

**الموقع:** `/src/components/SignaturePad.tsx`

**المميزات:**
- ✅ رسم باستخدام الماوس
- ✅ رسم باستخدام اللمس (موبايل)
- ✅ حفظ تلقائي للتوقيع
- ✅ مسح وإعادة التوقيع
- ✅ مؤشر مرئي للحالة

#### 2. `installment-agreement/page.tsx`
```typescript
// صفحة الاتفاقية الرئيسية
- 3 خطوات متسلسلة
- رفع صور
- SignaturePad integration
- إرسال لـ API
```

**الموقع:** `/src/app/installment-agreement/page.tsx`

**المكونات:**
- Progress bar للخطوات
- نص الاتفاقية مع التحذيرات
- شعار التطبيق
- نماذج رفع الصور
- لوحة التوقيع
- checkbox الموافقة

#### 3. `checkout/page.tsx` (محدثة)
```typescript
// تحديث على صفحة الدفع
- إضافة زر "الدفع بالتقسيط"
- عرض متطلبات التقسيط
- مثال على القسط الشهري
- توجيه لصفحة الاتفاقية
```

---

### Backend

#### API Route: `/api/installment/agreement`

##### POST - إنشاء اتفاقية جديدة

**Request Body:**
```json
{
  "nationalIdImage": "https://cloudinary.com/...",
  "signature": "data:image/png;base64,...",
  "selfieImage": "https://cloudinary.com/...",
  "fullName": "أحمد محمد",
  "nationalId": "12345678901234",
  "totalAmount": 5000,
  "downPayment": 1500,
  "numberOfInstallments": 12,
  "monthlyInstallment": 291.67
}
```

**Response:**
```json
{
  "success": true,
  "agreement": {
    "id": "clx...",
    "agreementNumber": "AGR-ABC123-DEF456",
    "status": "DOCUMENTS_COMPLETE"
  }
}
```

**الوظائف:**
- ✅ التحقق من المستخدم (Authentication)
- ✅ التحقق من البيانات (Validation)
- ✅ توليد رقم اتفاقية فريد
- ✅ حفظ IP و User Agent
- ✅ حالة افتراضية: DOCUMENTS_COMPLETE

##### GET - جلب اتفاقيات المستخدم

**Response:**
```json
{
  "agreements": [
    {
      "id": "clx...",
      "agreementNumber": "AGR-ABC123-DEF456",
      "status": "APPROVED",
      "totalAmount": 5000,
      "downPayment": 1500,
      "monthlyInstallment": 291.67,
      "createdAt": "2026-02-15T...",
      "order": {
        "id": "...",
        "orderNumber": "...",
        "status": "DELIVERED"
      }
    }
  ]
}
```

---

## 📸 رفع الصور (Cloudinary)

### Configuration

```env
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloud_name
```

### Upload Function

```typescript
const uploadImage = async (file: File) => {
  const formData = new FormData();
  formData.append('file', file);
  formData.append('upload_preset', 'ml_default');
  
  const response = await fetch(
    `https://api.cloudinary.com/v1_1/${CLOUD_NAME}/image/upload`,
    {
      method: 'POST',
      body: formData
    }
  );
  
  const data = await response.json();
  return data.secure_url;
};
```

### الصور المطلوبة:
1. **nationalIdImage** - البطاقة الشخصية
2. **selfieImage** - صورة شخصية (سيلفي)

**الـ signature** يتم حفظه كـ base64 في قاعدة البيانات.

---

## 🎨 واجهة المستخدم

### التصميم

#### الألوان:
- **بنفسجي** (Purple) - للتقسيط
- **وردي** (Pink) - للتدرجات
- **أخضر** (Green) - للخطوات المكتملة
- **أزرق** (Blue) - للخطوة الحالية

#### الأيقونات:
- 🏦 - أيقونة التقسيط
- 📄 - البطاقة الشخصية
- ✍️ - التوقيع
- 📸 - السيلفي
- ✅ - مكتمل
- ⚠️ - تحذير

#### Progress Bar:
```
[1] -------- [2] -------- [3]
 ✓           ✓           ( )
```

---

## ⚠️ التحذيرات القانونية

### النص في الاتفاقية:

```
⚠️ تحذير قانوني:

بموجب القانون المصري، أنت ملتزم بسداد جميع الأقساط في المواعيد المحددة.
عدم الالتزام بالسداد قد يعرضك للمساءلة القانونية والعقوبات التالية:

• غرامات تأخير تصل إلى 10% من قيمة القسط
• الإبلاغ عن سجلك الائتماني
• اتخاذ إجراءات قانونية ضدك
• حجز الممتلكات في حالة عدم السداد
```

---

## 🔐 الأمان

### حماية البيانات:
- ✅ جميع الصور على Cloudinary (محمية)
- ✅ التوقيع محفوظ بشكل آمن
- ✅ حفظ IP address
- ✅ حفظ User Agent
- ✅ Timestamps دقيقة
- ✅ لا يمكن تعديل الاتفاقية بعد الإرسال

### التحقق:
- Authentication مطلوب
- Validation على كل البيانات
- حجم الصور (max 5MB)
- نوع الملفات (images only)

---

## 📊 حسابات التقسيط

### صيغة الحساب:

```typescript
// المعطيات
const totalAmount = 5000;  // المبلغ الإجمالي
const downPaymentPercent = 30; // نسبة المقدم (30%)
const numberOfInstallments = 12; // عدد الأقساط
const interestRate = 0; // نسبة الفائدة (0% حالياً)

// الحسابات
const downPayment = totalAmount * (downPaymentPercent / 100);
// = 5000 * 0.3 = 1500 ج

const remainingAmount = totalAmount - downPayment;
// = 5000 - 1500 = 3500 ج

const monthlyInstallment = remainingAmount / numberOfInstallments;
// = 3500 / 12 = 291.67 ج/شهر
```

### مثال عملي:

| البيان | القيمة |
|--------|---------|
| **المبلغ الإجمالي** | 5,000 ج |
| **المقدم (30%)** | 1,500 ج |
| **المبلغ المتبقي** | 3,500 ج |
| **عدد الأقساط** | 12 شهر |
| **القسط الشهري** | 291.67 ج |
| **الفائدة** | 0% |

---

## 🔄 دورة الحياة الكاملة

### 1. العميل يختار التقسيط
```
[Checkout Page] 
→ Click "ابدأ اتفاقية التقسيط"
→ Router.push to /installment-agreement
```

### 2. صفحة الاتفاقية
```
Step 1: رفع البطاقة ✅
  ↓
Step 2: التوقيع ✅
  ↓
Step 3: السيلفي + الموافقة ✅
```

### 3. إرسال الاتفاقية
```
- Upload images to Cloudinary
- POST /api/installment/agreement
- Status: DOCUMENTS_COMPLETE
- Redirect to /checkout?agreementId=XXX
```

### 4. إتمام الطلب (في Checkout)
```
- العميل يكمل بيانات التوصيل
- Submit Order مع agreementId
- Order Status: PENDING
- Agreement Status: UNDER_REVIEW
```

### 5. مراجعة الإدارة
```
Admin Dashboard:
- مراجعة المستندات
- التحقق من البيانات
- قبول ✅ أو رفض ❌
```

### 6. الموافقة
```
IF APPROVED:
  - Agreement Status: APPROVED
  - Order Status: CONFIRMED
  - إنشاء جدول الأقساط
  - إرسال إشعار للعميل

IF REJECTED:
  - Agreement Status: REJECTED
  - Order Status: CANCELLED
  - إشعار بالسبب
```

---

## 🛠️ الصيانة والتطوير

### إضافة فترات تقسيط جديدة:

```typescript
// في checkout/page.tsx
const installmentOptions = [
  { months: 6, downPayment: 30 },
  { months: 12, downPayment: 30 },
  { months: 18, downPayment: 30 },  // ✨ جديد
  { months: 24, downPayment: 30 }
];
```

### إضافة فوائد:

```typescript
// في API
const interestRate = 5; // 5% سنوياً

const totalWithInterest = remainingAmount * (1 + (interestRate / 100));
const monthlyInstallment = totalWithInterest / numberOfInstallments;
```

### تخصيص نسبة المقدم:

```typescript
// حسب المبلغ
const downPaymentPercent = totalAmount > 10000 ? 40 : 30;
```

---

## 📱 الدعم والموبايل

### Responsive Design:
- ✅ موبايل فرست
- ✅ Touch support للـ signature
- ✅ Camera capture للسيلفي
- ✅ تحسين للشاشات الصغيرة

### PWA Support:
```javascript
// في الموبايل
<Input
  type="file"
  accept="image/*"
  capture="user"  // 📸 فتح الكاميرا مباشرة
/>
```

---

## ✅ Checklist التفعيل

### قبل التشغيل:

- [x] Schema updated
- [x] Database migrated (`npx prisma db push`)
- [x] Cloudinary configured
- [x] SignaturePad component tested
- [x] Agreement page created
- [x] API endpoint tested
- [x] Checkout page updated

### الاختبار:

- [ ] Flow كامل من Checkout للاتفاقية
- [ ] رفع صور نموذجية
- [ ] التوقيع على موبايل وديسكتوب
- [ ] حفظ البيانات في DB
- [ ] التحقق من Cloudinary URLs
- [ ] اختبار الـ Validation

### Production:

- [ ] تفعيل المراجعة الإدارية
- [ ] إعداد Notifications
- [ ] إنشاء لوحة Admin
- [ ] تدريب فريق الدعم
- [ ] مراجعة قانونية للاتفاقية

---

## 🎯 المميزات المستقبلية

### Phase 2:
- [ ] لوحة Admin لمراجعة الاتفاقيات
- [ ] تحقق تلقائي من البطاقة (OCR)
- [ ] مطابقة الصورة الشخصية مع البطاقة (Face Recognition)
- [ ] تذكير تلقائي بالأقساط (SMS/Email)
- [ ] دفع الأقساط أونلاين

### Phase 3:
- [ ] تكامل مع شركات الائتمان
- [ ] تقارير ائتمانية
- [ ] نظام نقاط ائتمانية
- [ ] خصومات للملتزمين
- [ ] برنامج ولاء للتقسيط

---

## 📞 الدعم الفني

### مشاكل شائعة:

**1. الصورة لا ترفع:**
- تحقق من حجم الصورة (< 5MB)
- تحقق من نوع الملف (image/*)
- تحقق من Cloudinary configuration

**2. التوقيع لا يظهر:**
- تحقق من Canvas API support
- اختبر على جهاز آخر
- Clear browser cache

**3. الموافقة لا تعمل:**
- تحقق من checkbox checked
- تحقق من validation logic
- راجع console errors

---

## 🎉 الخلاصة

نظام تقسيط متطور وآمن مع:
- ✅ اتفاقية قانونية مُلزمة
- ✅ 3 خطوات توثيق إجبارية
- ✅ توقيع إلكتروني فوري
- ✅ حماية كاملة للبيانات
- ✅ واجهة سهلة وجذابة
- ✅ دعم كامل للموبايل

**🚀 جاهز للتشغيل!**
