# نظام البحث والمحادثات - دليل الاستخدام

## 🔍 نظام البحث المتقدم

### الميزات
1. **البحث بالنص**: ابحث عن المنتجات بكتابة اسم المنتج
2. **البحث بالصورة**: ارفع صورة المنتج للبحث عن منتجات مشابهة

### الاستخدام
- الموقع: الصفحة الرئيسية (بين Hero Slider والفئات)
- يدعم JPG, PNG, GIF
- تصميم متجاوب للموبايل

### المكونات
- `src/components/SearchBar.tsx`: مكون البحث الرئيسي
- يتكامل مع `/api/upload` لرفع الصور

---

## 💬 نظام المحادثات

### للعملاء
- **زر المحادثة العائم**: يظهر في أسفل اليسار من كل صفحة
- **الإشعارات**: عداد للرسائل غير المقروءة
- **التحديث التلقائي**: يحدث كل 10 ثواني
- **الرسائل الفورية**: إرسال واستقبال فوري

### للإدارة
- **صفحة المحادثات**: `/admin/messages`
- **قائمة العملاء**: عرض جميع المحادثات
- **عداد غير المقروءة**: لكل محادثة
- **تحديث تلقائي**: كل 5 ثوانٍ للمحادثة المفتوحة

### المكونات
1. **ChatButton.tsx**: زر المحادثة العائم للعملاء
2. **AdminMessagesPage**: صفحة الإدارة الكاملة
3. **API Endpoints**:
   - `GET /api/messages`: جلب المحادثات
   - `POST /api/messages`: إرسال رسالة
   - `PATCH /api/messages`: تحديث حالة القراءة

---

## 🗄️ قاعدة البيانات

### جدول Messages
```prisma
model Message {
  id             String   @id @default(cuid())
  content        String   // محتوى الرسالة
  senderId       String   // معرف المرسل
  receiverId     String?  // معرف المستلم
  isRead         Boolean  @default(false)
  isAdminMessage Boolean  @default(false)
  orderId        String?  // ربط بطلب معين
  attachmentUrl  String?  // رفع ملفات
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
  
  @@index([senderId])
  @@index([receiverId])
}
```

---

## 📱 التصميم المتجاوب

### الموبايل
- نصوص أصغر (text-xs → text-sm)
- أزرار مضغوطة (h-9 → h-10)
- نوافذ ملء الشاشة
- تباعد مخصص (gap-2 → gap-4)

### الديسكتوب
- نوافذ ثابتة الحجم
- تفاصيل أكثر
- تخطيط متعدد الأعمدة

---

## 🎨 التصميم

### الألوان
- خلفية: `from-gray-900 via-teal-900 to-gray-900`
- أزرار أساسية: `bg-teal-600 hover:bg-teal-700`
- رسائل المرسل: `bg-teal-600`
- رسائل المستقبل: `bg-gray-800`

### الأيقونات
- بحث: `Search`
- كاميرا: `Camera`
- محادثة: `MessageCircle`
- إرسال: `Send`
- مستخدم: `User`

---

## 🚀 الخطوات التالية (اختياري)

1. **AI للبحث بالصورة**:
   - تكامل مع OpenAI Vision API
   - البحث بالتشابه البصري

2. **الإشعارات الفورية**:
   - WebSocket للرسائل الفورية
   - Push notifications

3. **الملفات المرفقة**:
   - إرسال صور في المحادثات
   - مشاركة ملفات PDF

4. **التحليلات**:
   - عدد الرسائل اليومي
   - متوسط وقت الرد
   - رضا العملاء

---

## 📋 قائمة التحقق

✅ إضافة Message model إلى Prisma  
✅ إنشاء migration للجداول  
✅ API endpoints للمحادثات (GET, POST, PATCH)  
✅ مكون SearchBar مع رفع الصور  
✅ مكون ChatButton للعملاء  
✅ صفحة المحادثات للإدارة  
✅ إضافة المكونات للصفحة الرئيسية  
✅ تصميم متجاوب كامل  
✅ تحديث تلقائي للمحادثات  
✅ إدارة حالة القراءة  

---

## 🎯 كيفية الاختبار

1. **سجل دخول كعميل**
2. **انقر على زر المحادثة** (أسفل اليسار)
3. **اكتب رسالة** وأرسلها
4. **سجل خروج ودخول كمدير**
5. **اذهب إلى** `/admin/messages`
6. **شاهد المحادثة** ورد عليها
7. **ارجع للعميل** وشاهد الرد

---

## 💡 نصائح

- **للإدارة**: راقب عداد الرسائل غير المقروءة
- **للعملاء**: استخدم المحادثة لأي استفسار
- **البحث**: جرب البحث بصورة منتج من جوجل
- **الموبايل**: النافذة تتكيف مع حجم الشاشة تلقائيًا

---

تم التطوير بواسطة GitHub Copilot ✨
