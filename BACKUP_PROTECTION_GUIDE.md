# 🛡️ نظام حماية البيانات - Backup Protection System

## 📋 الميزات الأمنية

### 1. ✅ Backup يومي أوتوماتيكي
- **التوقيت**: كل يوم الساعة 5 صباحاً بتوقيت مصر
- **الموقع**: مجلد `backups/`
- **التنفيذ**: عبر GitHub Actions
- **الحفظ**: تلقائياً على GitHub

### 2. 🔒 حماية بالباسورد
- **كلمة المرور**: `Remo@2026!DeleteProtection`
- **التطبيق**: على جميع عمليات الحذف
- **الإلزام**: لا يمكن الحذف بدون الباسورد

### 3. 💾 Backup إجباري قبل الحذف
- **تلقائي**: يتم عمل backup قبل أي حذف
- **التحقق**: لا يمكن الحذف إذا فشل الbackup
- **الحفظ**: في مجلد `backups/` مع timestamp

### 4. ⏰ تنظيف تلقائي
- **المدة**: حذف backups أقدم من 30 يوم
- **السبب**: توفير المساحة
- **الأمان**: الbackups الحديثة محمية

---

## 🚀 كيفية الاستخدام

### تشغيل Backup يدوياً:
```bash
npx tsx auto-backup-daily.ts
```

### استرجاع بيانات من Backup:
```bash
npx tsx restore-from-backup.ts
```

### حماية أي script حذف:
```typescript
import { requirePasswordBeforeDelete, createBackupBeforeDelete, confirmDeletion } from './safe-delete-protection';

async function deleteProducts() {
  // 1. طلب الباسورد
  if (!(await requirePasswordBeforeDelete('حذف المنتجات'))) {
    process.exit(1);
  }

  // 2. عمل backup
  await createBackupBeforeDelete('حذف منتجات تجريبية');

  // 3. تأكيد نهائي
  if (!(await confirmDeletion(50, 'منتج'))) {
    process.exit(1);
  }

  // 4. الحذف الآمن
  await prisma.product.deleteMany();
}
```

---

## 📂 مواقع الBackups

1. **Local**: `D:\markting\backups\`
2. **GitHub**: في نفس الrepo
3. **Vercel**: غير متوفر (Database منفصلة)

---

## 🔑 معلومات مهمة

### كلمة مرور الحماية:
```
Remo@2026!DeleteProtection
```

### استرجاع البيانات:
1. افتح مجلد `backups/`
2. اختر الbackup المطلوب
3. شغل: `npx tsx restore-from-backup.ts`

### عرض كل الBackups:
```bash
cd backups
ls -la
```

---

## ⚠️ تحذيرات

- ❌ **لا تحذف** مجلد `backups/`
- ❌ **لا تشارك** كلمة المرور
- ✅ **احتفظ** بنسخ الbackups في مكان آمن
- ✅ **راجع** الbackups بشكل دوري

---

## 📊 إحصائيات الBackup الحالي

تحقق من آخر backup:
```bash
npx tsx check-latest-backup.ts
```

---

## 🆘 في حالة الطوارئ

إذا تم حذف البيانات:

1. **لا تقلق!** الbackups موجودة
2. افتح `backups/` واختر آخر نسخة
3. شغل: `npx tsx restore-from-backup.ts`
4. تأكد من البيانات: `npx tsx check-all-data.ts`

---

## 📞 الدعم

- WhatsApp: 01555512778
- Email: remostore.egy@gmail.com

---

**✅ بياناتك محمية 100%! 🛡️**
