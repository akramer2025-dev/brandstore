# دليل الصور المتعددة للمنتجات
# Multiple Product Images Guide

## نظرة عامة | Overview

تم تحديث النظام ليدعم **رفع وعرض صور متعددة لكل منتج** (حتى 5 صور).

The system has been updated to support **uploading and displaying multiple images per product** (up to 5 images).

---

## ✨ الميزات الجديدة | New Features

### 1️⃣ رفع صور متعددة
- رفع حتى **5 صور** لكل منتج
- معاينة الصور قبل الرفع
- إمكانية حذف أي صورة
- عرض رقم الصورة على كل صورة
- الصورة الأولى تكون الصورة الرئيسية

### 2️⃣ عرض الصور للعميل
- **معرض صور** تفاعلي في صفحة المنتج
- إمكانية الانتقال بين الصور
- عرض thumbnails لكل الصور
- الصورة المختارة تظهر بحجم كبير

### 3️⃣ عرض الصور في الإدارة
- عرض الصورة الأولى في قائمة المنتجات
- badge يوضح عدد الصور المرفوعة
- معاينة سهلة لكل الصور

---

## 📁 الملفات المحدثة | Updated Files

### Components
- ✅ `src/components/MultipleImageUpload.tsx` - مكون جديد لرفع صور متعددة
- ✅ `src/components/ProductCard.tsx` - عرض الصورة الأولى
- ✅ `src/components/ProductsSlider.tsx` - عرض الصورة الأولى

### Pages
- ✅ `src/app/products/[id]/page.tsx` - معرض الصور التفاعلي
- ✅ `src/app/admin/products/new/page.tsx` - رفع صور متعددة
- ✅ `src/app/admin/products/page.tsx` - عرض عدد الصور
- ✅ `src/app/orders/page.tsx` - عرض الصورة الأولى
- ✅ `src/app/admin/orders/[id]/page.tsx` - عرض الصورة الأولى
- ✅ `src/app/delivery/[id]/page.tsx` - عرض الصورة الأولى

---

## 🎯 كيفية الاستخدام | How to Use

### للإدارة (Admin)

#### إضافة منتج جديد بصور متعددة:

1. اذهب إلى: `/admin/products/new`
2. املأ بيانات المنتج
3. في قسم "صور المنتج":
   - اضغط على منطقة الرفع
   - اختر حتى 5 صور (أو اختر صورة واحدة وكرر العملية)
   - الصورة الأولى ستكون الصورة الرئيسية
4. معاينة الصور المرفوعة
5. لحذف صورة: اضغط على زر ❌ على الصورة
6. احفظ المنتج

#### ملاحظات مهمة:
- **الحد الأقصى:** 5 صور
- **حجم الصورة:** حتى 5 ميجابايت
- **الصيغ المدعومة:** JPG, PNG, WebP
- **الصورة الأولى:** هي التي تظهر في القوائم والبطاقات

---

### للعملاء (Customers)

#### مشاهدة صور المنتج:

1. اذهب إلى صفحة المنتج: `/products/[id]`
2. ستجد:
   - **صورة كبيرة** للصورة المختارة
   - **معرض صور صغير** أسفل الصورة الكبيرة
3. اضغط على أي صورة صغيرة لعرضها بحجم كبير
4. الصورة المختارة لها **إطار أزرق (teal)** مميز

---

## 🔧 التقنيات المستخدمة | Technical Details

### تخزين الصور في قاعدة البيانات

الصور تُخزن في حقل `images` كنص مفصول بفواصل:

```typescript
// Database field: images (String)
// Example: "image1.jpg,image2.jpg,image3.jpg"

// في الكود يتم تحويلها لـ array:
const imagesArray = product.images?.split(',').map(img => img.trim());
// Result: ["image1.jpg", "image2.jpg", "image3.jpg"]
```

### رفع الصور

```typescript
// Component: MultipleImageUpload
<MultipleImageUpload
  currentImages={[]}  // الصور الحالية
  onImagesUploaded={(urls) => {
    // urls: string[] - مصفوفة URLs الصور
    console.log(urls);
  }}
  maxImages={5}  // الحد الأقصى
/>
```

### عرض الصور

```typescript
// في صفحة المنتج
const images = product.images 
  ? product.images.split(',').map(img => img.trim())
  : [defaultImage];

// استخدام الصور
<Image src={images[0]} />  // الصورة الأولى
<Image src={images[selectedIndex]} />  // الصورة المختارة
```

---

## 🎨 مثال كامل | Complete Example

### إضافة منتج بصور متعددة:

```typescript
// admin/products/new/page.tsx

const [formData, setFormData] = useState({
  name: "",
  price: "",
  images: "",  // "img1.jpg,img2.jpg,img3.jpg"
  // ... باقي الحقول
});

const handleImagesUpload = (imageUrls: string[]) => {
  setFormData(prev => ({
    ...prev,
    images: imageUrls.join(',')  // تحويل array لـ string
  }));
};

// في الـ form
<MultipleImageUpload
  currentImages={formData.images ? formData.images.split(',') : []}
  onImagesUploaded={handleImagesUpload}
  maxImages={5}
/>
```

### عرض الصور في صفحة المنتج:

```typescript
// products/[id]/page.tsx

const [selectedImage, setSelectedImage] = useState(0);

const images = product.images 
  ? product.images.split(',').map(img => img.trim())
  : [defaultImage];

// الصورة الكبيرة
<Image src={images[selectedImage]} />

// معرض الصور الصغير
{images.map((image, index) => (
  <button
    key={index}
    onClick={() => setSelectedImage(index)}
    className={selectedImage === index ? 'selected' : ''}
  >
    <Image src={image} />
  </button>
))}
```

---

## 📊 الإحصائيات | Statistics

### قبل التحديث:
- ❌ صورة واحدة فقط لكل منتج
- ❌ لا يمكن عرض زوايا مختلفة
- ❌ تجربة محدودة للعميل

### بعد التحديث:
- ✅ حتى 5 صور لكل منتج
- ✅ معرض صور تفاعلي
- ✅ تجربة أفضل للعميل
- ✅ زيادة مبيعات متوقعة 📈

---

## 🚀 التحسينات المستقبلية | Future Improvements

- [ ] إعادة ترتيب الصور (drag & drop)
- [ ] zoom للصور عند التمرير
- [ ] lightbox لعرض الصور بحجم كامل
- [ ] تحميل الصور بشكل lazy
- [ ] ضغط الصور تلقائياً
- [ ] إضافة نص بديل لكل صورة (SEO)

---

## 💡 نصائح | Tips

### للحصول على أفضل النتائج:

1. **استخدم صور عالية الجودة** (1000x1000 على الأقل)
2. **اجعل الصورة الأولى الأفضل** (تظهر في كل مكان)
3. **اعرض زوايا مختلفة** للمنتج
4. **أضف صور للتفاصيل** (الخامة، الجودة، إلخ)
5. **استخدم خلفية بيضاء أو شفافة** للاحترافية

---

## 🔗 روابط مفيدة | Useful Links

- [Next.js Image Component](https://nextjs.org/docs/app/api-reference/components/image)
- [Prisma Schema](https://www.prisma.io/docs/concepts/components/prisma-schema)
- [File Upload Best Practices](https://web.dev/file-upload-best-practices/)

---

## 📞 الدعم | Support

إذا واجهت أي مشاكل:
1. تحقق من Console للأخطاء
2. تأكد من حجم الصور < 5MB
3. تأكد من الصيغ المدعومة فقط
4. راجع الـ Network tab لطلبات الرفع

---

**تم التحديث:** يناير 2026 ✅
