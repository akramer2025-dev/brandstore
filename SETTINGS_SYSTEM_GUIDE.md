# نظام إدارة الإعدادات والسلايدر

## 📋 نظرة عامة

تم إضافة نظام كامل لإدارة إعدادات الموقع وصور السلايدر الرئيسية، يتيح للمدير التحكم الكامل في مظهر وإعدادات الموقع.

---

## ✅ الميزات المضافة

### 1️⃣ **نظام الإعدادات العامة** (`/admin/settings`)

#### الوظائف:
- إدارة إعدادات الموقع بشكل مركزي
- تنظيم الإعدادات في 4 فئات:
  - **إعدادات عامة**: اسم الموقع، البريد، الهاتف، العنوان
  - **المظهر**: اللون الرئيسي، إظهار السلايدر، عدد المنتجات
  - **SEO**: Meta title, description, keywords
  - **وسائل التواصل**: روابط Facebook, Instagram, Twitter

#### قاعدة البيانات:
```prisma
model SiteSetting {
  id          String   @id @default(cuid())
  key         String   @unique
  value       String
  type        String   @default("text") // text, number, boolean, json
  category    String   @default("general")
  description String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
```

#### الإعدادات الافتراضية:
```
- site_name: "متجر شامل"
- site_name_en: "Comprehensive Store"
- contact_email: "info@store.com"
- contact_phone: "+20 123 456 7890"
- contact_whatsapp: "+201234567890"
- primary_color: "#3b82f6"
- show_slider: "true"
- products_per_page: "12"
- facebook_url, instagram_url, twitter_url
```

---

### 2️⃣ **نظام إدارة السلايدر** (`/admin/settings/slider`)

#### الوظائف:
- ✅ إضافة صور سلايدر جديدة
- ✅ تعديل صور السلايدر الموجودة
- ✅ حذف الصور
- ✅ ترتيب الصور (أعلى/أسفل)
- ✅ تفعيل/إيقاف الصور
- ✅ دعم اللغتين (عربي/إنجليزي)
- ✅ إضافة روابط وأزرار CTA

#### قاعدة البيانات:
```prisma
model SliderImage {
  id           String   @id @default(cuid())
  title        String
  titleAr      String?
  subtitle     String?
  subtitleAr   String?
  imageUrl     String
  link         String?
  buttonText   String?
  buttonTextAr String?
  order        Int      @default(0)
  isActive     Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}
```

#### صور السلايدر الافتراضية:
1. **عروض الصيف الكبرى** - خصومات 50%
2. **مستلزمات المدرسة** - أدوات مكتبية
3. **أحدث صيحات الموضة** - ملابس عصرية
4. **توصيل مجاني** - فوق 500 جنيه

---

## 🚀 كيفية الاستخدام

### الوصول للإعدادات:

1. **من لوحة الأدمن الرئيسية:**
   ```
   http://localhost:3000/admin
   ```
   - ابحث عن كارت **"⚙️ إعدادات الموقع"**
   - اضغط للوصول لصفحة الإعدادات

2. **مباشرة:**
   ```
   http://localhost:3000/admin/settings
   http://localhost:3000/admin/settings/slider
   ```

---

### إدارة الإعدادات العامة:

#### 1. تعديل الإعدادات:
```
1. اذهب إلى: /admin/settings
2. اختر التبويب المناسب (عام، المظهر، SEO، وسائل التواصل)
3. عدل القيم المطلوبة
4. اضغط "حفظ التغييرات"
```

#### 2. أنواع الإعدادات:
- **text**: نصوص عادية (اسم الموقع، البريد، الروابط)
- **number**: أرقام (عدد المنتجات، الأسعار)
- **boolean**: نعم/لا (إظهار السلايدر، تفعيل ميزة)
- **json**: بيانات معقدة (إعدادات متقدمة)

---

### إدارة السلايدر:

#### 1. إضافة صورة جديدة:
```
1. اذهب إلى: /admin/settings/slider
2. اضغط "إضافة صورة جديدة"
3. املأ البيانات:
   - العنوان (عربي وإنجليزي)
   - الوصف (عربي وإنجليزي)
   - رابط الصورة
   - الرابط عند الضغط (مثال: /products?category=cat1)
   - نص الزر (مثال: تسوق الآن)
4. اضغط "حفظ"
```

#### 2. تعديل صورة:
```
1. ابحث عن الصورة في القائمة
2. اضغط أيقونة التعديل (قلم)
3. عدل البيانات المطلوبة
4. احفظ التغييرات
```

#### 3. ترتيب الصور:
```
- استخدم أسهم (↑↓) لتحريك الصور للأعلى أو الأسفل
- الترتيب يحدد ظهور الصور في الصفحة الرئيسية
```

#### 4. تفعيل/إيقاف صورة:
```
- اضغط أيقونة العين 👁️ لتفعيل الصورة
- اضغط أيقونة العين المشطوبة 🚫 لإيقاف الصورة
- الصور الموقوفة لن تظهر في الصفحة الرئيسية
```

#### 5. حذف صورة:
```
- اضغط أيقونة سلة المهملات 🗑️
- أكد الحذف
```

---

## 🔌 API Endpoints

### الإعدادات:
```typescript
// GET - جلب جميع الإعدادات
GET /api/admin/settings

// PUT - تحديث الإعدادات
PUT /api/admin/settings
Body: { settings: [...] }
```

### السلايدر:
```typescript
// GET - جلب جميع الصور
GET /api/admin/slider

// POST - إضافة صورة جديدة
POST /api/admin/slider
Body: { title, titleAr, imageUrl, ... }

// PUT - تحديث صورة
PUT /api/admin/slider
Body: { id, ...updateData }

// DELETE - حذف صورة
DELETE /api/admin/slider
Body: { id }
```

---

## 💾 هيكل البيانات

### إعداد واحد:
```json
{
  "id": "clx...",
  "key": "site_name",
  "value": "متجر شامل",
  "type": "text",
  "category": "general",
  "description": "اسم الموقع",
  "createdAt": "2024-01-08T12:00:00Z",
  "updatedAt": "2024-01-08T12:00:00Z"
}
```

### صورة سلايدر واحدة:
```json
{
  "id": "clx...",
  "title": "Summer Sale",
  "titleAr": "عروض الصيف الكبرى",
  "subtitle": "Up to 50% off",
  "subtitleAr": "خصومات تصل إلى 50%",
  "imageUrl": "https://...",
  "link": "/products?category=cat1",
  "buttonText": "Shop Now",
  "buttonTextAr": "تسوق الآن",
  "order": 1,
  "isActive": true,
  "createdAt": "2024-01-08T12:00:00Z",
  "updatedAt": "2024-01-08T12:00:00Z"
}
```

---

## 🎨 أمثلة عملية

### مثال 1: تغيير اسم الموقع
```
1. اذهب لـ /admin/settings
2. في تبويب "إعدادات عامة"
3. غير "اسم الموقع" من "متجر شامل" إلى "متجرك الجديد"
4. غير "Site name in English" إلى "Your New Store"
5. احفظ التغييرات
```

### مثال 2: إضافة عرض خاص في السلايدر
```
1. اذهب لـ /admin/settings/slider
2. اضغط "إضافة صورة جديدة"
3. املأ:
   - العنوان: "خصم 70% على الملابس"
   - Title: "70% Off Clothes"
   - رابط الصورة: https://example.com/promo.jpg
   - الرابط: /products?category=cat1
   - نص الزر: "تسوق الآن" / "Shop Now"
4. احفظ
```

### مثال 3: تعديل روابط وسائل التواصل
```
1. اذهب لـ /admin/settings
2. في تبويب "وسائل التواصل"
3. أدخل روابط صفحاتك:
   - Facebook: https://facebook.com/yourpage
   - Instagram: https://instagram.com/yourpage
   - Twitter: https://twitter.com/yourpage
4. احفظ التغييرات
```

---

## 🔐 الأمان

- ✅ الوصول محمي - مدير النظام فقط (ADMIN)
- ✅ التحقق من الجلسة (NextAuth Session)
- ✅ Prisma ORM - حماية من SQL Injection
- ✅ التحقق من البيانات قبل الحفظ

---

## 📊 الإحصائيات الحالية

### قاعدة البيانات:
- ✅ 16 إعداد افتراضي
- ✅ 4 صور سلايدر افتراضية
- ✅ جميع البيانات مزودة بـ seed

### الملفات المضافة:
```
✅ prisma/schema.prisma - نماذج SiteSetting و SliderImage
✅ src/app/admin/settings/page.tsx - صفحة الإعدادات
✅ src/app/admin/settings/slider/page.tsx - صفحة السلايدر
✅ src/app/api/admin/settings/route.ts - API الإعدادات
✅ src/app/api/admin/slider/route.ts - API السلايدر
✅ src/app/admin/page.tsx - إضافة كارت الإعدادات
```

---

## 🚀 الخطوات التالية (اختياري)

1. **دمج السلايدر في الصفحة الرئيسية**:
   - تعديل `/src/app/page.tsx`
   - عرض صور السلايدر الفعالة
   - إضافة carousel component

2. **استخدام الإعدادات في الموقع**:
   ```typescript
   // في أي component
   const settings = await prisma.siteSetting.findMany();
   const siteName = settings.find(s => s.key === 'site_name')?.value;
   ```

3. **رفع الصور محليًا**:
   - إضافة نظام رفع الصور
   - حفظ في `/public/uploads/slider`
   - عرض الصور من السيرفر المحلي

---

## 📝 ملاحظات مهمة

### بيانات الدخول:
```
المدير: admin@store.com / admin123
```

### الروابط المهمة:
```
الصفحة الرئيسية: http://localhost:3000
لوحة الأدمن: http://localhost:3000/admin
الإعدادات: http://localhost:3000/admin/settings
السلايدر: http://localhost:3000/admin/settings/slider
```

### قاعدة البيانات:
```
sqlite: prisma/dev.db
Studio: npx prisma studio
```

---

## 🎉 نظام الإعدادات جاهز للاستخدام!

يمكنك الآن:
1. ✅ إدارة جميع إعدادات الموقع من مكان واحد
2. ✅ التحكم في صور السلايدر الرئيسية
3. ✅ تعديل معلومات التواصل وSEO
4. ✅ تخصيص المظهر والألوان
5. ✅ إضافة حملات ترويجية في السلايدر

---

**تم بنجاح! 🚀**
