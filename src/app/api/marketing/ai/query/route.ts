import { NextRequest, NextResponse } from "next/server";
import { auth } from "@/lib/auth";

export async function POST(req: NextRequest) {
  try {
    const session = await auth();

    if (!session || session.user?.role !== "ADMIN") {
      return NextResponse.json({ error: "غير مصرح" }, { status: 401 });
    }

    const { query, campaigns, keywords, analytics } = await req.json();

    // Generate AI response based on user query and data
    const response = generateAIResponse(query, campaigns, keywords, analytics);

    return NextResponse.json({ response });
  } catch (error) {
    console.error("AI Query Error:", error);
    return NextResponse.json({ error: "فشل معالجة الاستفسار" }, { status: 500 });
  }
}

function generateAIResponse(query: string, campaigns: any[], keywords: any[], analytics: any[]) {
  const lowerQuery = query.toLowerCase();

  // Calculate key metrics
  const totalCampaigns = campaigns.length;
  const activeCampaigns = campaigns.filter(c => c.status === "ACTIVE").length;
  const totalSpent = campaigns.reduce((sum, c) => sum + c.spent, 0);
  const totalConversions = campaigns.reduce((sum, c) => sum + c.conversions, 0);
  const avgROI = campaigns.length > 0 ? campaigns.reduce((sum, c) => sum + c.roi, 0) / campaigns.length : 0;
  const avgCTR = campaigns.length > 0 ? campaigns.reduce((sum, c) => sum + c.ctr, 0) / campaigns.length : 0;

  // ROI-related queries
  if (lowerQuery.includes("roi") || lowerQuery.includes("عائد") || lowerQuery.includes("ربح")) {
    return `💰 **تحليل العائد على الاستثمار (ROI)**

📊 **الوضع الحالي:**
- متوسط ROI: ${avgROI.toFixed(0)}%
- إجمالي الإنفاق: ${totalSpent.toFixed(0)} ج
- إجمالي التحويلات: ${totalConversions}

${avgROI < 100 ? `
⚠️ **تحذير:** ROI أقل من 100% يعني أنك تخسر مالاً!

🔧 **حلول سريعة لزيادة ROI:**
1. أوقف الحملات ذات ROI أقل من 50% فوراً
2. ضاعف ميزانية الحملات الناجحة (ROI > 150%)
3. حسّن Targeting للجمهور الأكثر استجابة
4. اختبر أسعار مختلفة (A/B Testing)
5. قلل تكلفة النقرة بتحسين Quality Score
` : `
✅ **أداء جيد!** ROI أعلى من 100%

📈 **لزيادة ROI أكثر:**
1. استثمر في الحملات الأفضل أداءً
2. جرّب Lookalike Audiences
3. استخدم Dynamic Ads لاستهداف أدق
4. حسّن سرعة موقعك لتقليل معدل الارتداد
`}

💡 **معادلة ROI:**
ROI = ((الإيرادات - التكلفة) / التكلفة) × 100

🎯 **الهدف المثالي:** 200%+ ROI`;
  }

  // CTR-related queries
  if (lowerQuery.includes("ctr") || lowerQuery.includes("نقر") || lowerQuery.includes("نقرات")) {
    return `🖱️ **تحسين معدل النقر (CTR)**

📊 **CTR الحالي:** ${avgCTR.toFixed(2)}%

${avgCTR < 2 ? `
⚠️ **CTR منخفض جداً!** (المعدل الطبيعي: 2-5%)

🚀 **طرق مجربة لزيادة CTR:**

1️⃣ **حسّن العنوان:**
- استخدم أرقام: "5 أسباب..."
- أضف كلمات قوية: "حصري، مجاني، الآن"
- اطرح سؤال: "هل تبحث عن...؟"

2️⃣ **الصورة/الفيديو:**
- استخدم صور عالية الجودة
- أضف نص على الصورة
- استخدم ألوان جذابة ومتباينة

3️⃣ **الوصف:**
- اذكر الفائدة الرئيسية بوضوح
- أضف Call-to-Action قوي
- استخدم الإيموجي بذكاء 🎯

4️⃣ **الاستهداف:**
- استهدف جمهور محدد أكثر
- استخدم الكلمات المفتاحية الدقيقة
- جرّب أوقات نشر مختلفة
` : `
✅ **CTR جيد!** أنت فوق المتوسط

💡 **لزيادته أكثر:**
- اختبر عناوين مختلفة (A/B Testing)
- استخدم Ad Extensions في Google
- أضف عروض محددة (خصم 30%)
- استخدم Urgency (متبقي 24 ساعة فقط!)
`}`;
  }

  // Conversion-related queries
  if (lowerQuery.includes("تحويل") || lowerQuery.includes("مبيعات") || lowerQuery.includes("conversion")) {
    return `🎯 **زيادة معدل التحويل**

📊 **التحويلات الحالية:** ${totalConversions}
💰 **تكلفة التحويل:** ${totalConversions > 0 ? (totalSpent / totalConversions).toFixed(0) : 0} ج

🚀 **استراتيجيات لزيادة التحويلات:**

1️⃣ **تحسين صفحة الهبوط:**
- عنوان واضح وجذاب
- صور عالية الجودة للمنتج
- زر Call-to-Action بارز (اشتري الآن)
- إزالة أي عوائق (تسجيل، خطوات كثيرة)
- أضف مراجعات العملاء ⭐⭐⭐⭐⭐

2️⃣ **بناء الثقة:**
- شهادات عملاء حقيقيين
- ضمان استرجاع المال
- أيقونات الأمان والدفع الآمن
- معلومات اتصال واضحة

3️⃣ **تقليل الاحتكاك:**
- خيار الشراء كضيف (بدون تسجيل)
- دفع سريع (One-Click)
- خيارات دفع متعددة
- شحن مجاني أو رخيص

4️⃣ **Urgency & Scarcity:**
- "متبقي قطعتين فقط!"
- "العرض ينتهي خلال 24 ساعة"
- عداد تنازلي
- "X شخص يشاهد هذا المنتج الآن"

5️⃣ **Retargeting:**
- استهدف من أضاف للسلة ولم يكمل
- أرسل تذكير بالبريد
- اعرض خصم إضافي 10%

6️⃣ **تحسين السرعة:**
- موقع سريع (أقل من 3 ثوان)
- متوافق مع الموبايل
- صور محسّنة

📈 **قياس النجاح:**
راقب: معدل الإضافة للسلة → معدل إتمام الشراء → الإيرادات`;
  }

  // Budget-related queries
  if (lowerQuery.includes("ميزانية") || lowerQuery.includes("budget") || lowerQuery.includes("تكلفة")) {
    return `💰 **إدارة ميزانية الحملات**

📊 **الوضع المالي الحالي:**
- إجمالي الميزانيات: ${campaigns.reduce((sum, c) => sum + c.budget, 0).toFixed(0)} ج
- إجمالي الإنفاق: ${totalSpent.toFixed(0)} ج
- الميزانية المتبقية: ${(campaigns.reduce((sum, c) => sum + c.budget, 0) - totalSpent).toFixed(0)} ج

💡 **استراتيجية توزيع الميزانية:**

1️⃣ **قاعدة 70-20-10:**
- 70% للحملات المجربة الناجحة
- 20% لتحسين الحملات المتوسطة
- 10% لتجربة حملات/منصات جديدة

2️⃣ **حسب الأداء:**
${campaigns.length > 0 ? campaigns
  .sort((a, b) => b.roi - a.roi)
  .slice(0, 3)
  .map((c, i) => `\n${i + 1}. "${c.name}": ROI ${c.roi.toFixed(0)}% → ${c.roi > 150 ? "زِد الميزانية" : c.roi > 100 ? "حافظ" : "قلل أو أوقف"}`)
  .join('') : ""}

3️⃣ **تقسيم حسب مراحل القمع:**
- 40% Top of Funnel (الوعي - Awareness)
- 30% Middle of Funnel (الاهتمام - Consideration)
- 30% Bottom of Funnel (الشراء - Conversion)

4️⃣ **ميزانية يومية مقابل إجمالية:**
- يومية: تحكم أفضل، مناسبة للميزانيات الصغيرة
- إجمالية: مرونة أكثر، مناسبة لحملات محددة الوقت

⚠️ **متى تزيد الميزانية:**
- ROI أعلى من 150%
- التحويلات تزيد باستمرار
- تصل لحد الإنفاق اليومي بسرعة

⚠️ **متى تقلل الميزانية:**
- ROI أقل من 100%
- تكلفة التحويل مرتفعة جداً
- CTR أقل من 1%

💡 **نصيحة:** ابدأ بميزانية صغيرة، اختبر، ثم ضاعف ما ينجح!`;
  }

  // Platform-related queries
  if (lowerQuery.includes("منصة") || lowerQuery.includes("فيسبوك") || lowerQuery.includes("جوجل") || 
      lowerQuery.includes("انستجرام") || lowerQuery.includes("platform")) {
    return `📱 **اختيار المنصة الإعلانية المناسبة**

🎯 **مقارنة المنصات:**

1️⃣ **Google Ads**
✅ الأفضل لـ:
- من يبحث عن منتج محدد (High Intent)
- B2B والخدمات
- الكلمات المفتاحية المحددة

💰 التكلفة: متوسطة إلى مرتفعة
📊 ROI: مرتفع عادةً

2️⃣ **Facebook/Instagram**
✅ الأفضل لـ:
- بناء الوعي بالعلامة التجارية
- المنتجات المرئية (ملابس، إكسسوارات)
- استهداف ديموغرافي دقيق

💰 التكلفة: منخفضة إلى متوسطة
📊 ROI: جيد للمنتجات الاستهلاكية

3️⃣ **TikTok**
✅ الأفضل لـ:
- الجمهور الشاب (16-30 سنة)
- المحتوى الإبداعي والمرح
- Viral Marketing

💰 التكلفة: منخفضة حالياً
📊 ROI: عالي جداً إذا كان المحتوى جيد

4️⃣ **Snapchat**
✅ الأفضل لـ:
- الجمهور في السعودية ومصر
- سن 13-25
- العروض السريعة

💰 التكلفة: متوسطة
📊 ROI: جيد في الشرق الأوسط

📊 **حسب بياناتك الحالية:**
${campaigns.length > 0 ? `
المنصات التي تستخدمها:
${Array.from(new Set(campaigns.map(c => c.platform))).map(p => `- ${p}`).join('\n')}

💡 **اقتراح:** جرّب منصة جديدة بميزانية صغيرة!
` : "لم تبدأ أي حملات بعد"}

🎯 **استراتيجية Multi-Platform:**
1. ابدأ بمنصة واحدة حتى تتقنها
2. عندما يستقر ROI > 150%، جرب منصة ثانية
3. استخدم Retargeting عبر المنصات
4. وحّد الرسالة التسويقية عبر كل المنصات`;
  }

  // SEO queries
  if (lowerQuery.includes("seo") || lowerQuery.includes("محركات البحث") || lowerQuery.includes("ترتيب")) {
    return `🔍 **تحسين محركات البحث (SEO)**

📊 **كلماتك المفتاحية الحالية:** ${keywords.length}

🚀 **خطة SEO الشاملة (3-6 أشهر):**

1️⃣ **البحث عن الكلمات (Keyword Research):**
- استخدم Google Keyword Planner
- ابحث عن Long-Tail Keywords (أقل منافسة)
- راقب كلمات المنافسين
- استهدف Local SEO (اسم مدينتك + منتجك)

2️⃣ **On-Page SEO:**
- عناوين صفحات واضحة (Title Tags)
- أوصاف meta جذابة
- عناوين H1, H2, H3 منظمة
- صور مع Alt Text
- URLs واضحة ونظيفة
- محتوى جودة عالية (500+ كلمة)

3️⃣ **Technical SEO:**
- سرعة الموقع (استخدم Google PageSpeed)
- Mobile-Friendly (responsive design)
- خريطة الموقع (Sitemap)
- ربط صحيح (Internal Linking)
- HTTPS (شهادة أمان)

4️⃣ **Off-Page SEO:**
- روابط خارجية (Backlinks) من مواقع موثوقة
- تواجد على السوشيال ميديا
- تسجيل في Google My Business
- مراجعات العملاء

5️⃣ **Content Strategy:**
- مدونة مع مقالات مفيدة
- دليل شراء المنتجات
- فيديوهات توضيحية
- FAQs (أسئلة شائعة)

📈 **قياس النجاح:**
- Google Search Console
- Google Analytics
- تتبع ترتيب كلماتك أسبوعياً
- مراقبة الزيارات العضوية

⏱️ **المدة:** SEO يحتاج صبر! النتائج تظهر بعد 3-6 أشهر

💡 **نصيحة:** ركز على تجربة المستخدم - Google يكافئ المواقع المفيدة!`;
  }

  // General marketing advice
  return `🤖 **إجابة الذكاء الاصطناعي**

شكراً لسؤالك: "${query}"

📊 **تحليل سريع لوضعك الحالي:**
- إجمالي الحملات: ${totalCampaigns}
- الحملات النشطة: ${activeCampaigns}
- متوسط ROI: ${avgROI.toFixed(0)}%
- إجمالي الإنفاق: ${totalSpent.toFixed(0)} ج
- التحويلات: ${totalConversions}

${totalCampaigns === 0 ? `
🚀 **ابدأ رحلتك التسويقية:**
1. حدد هدفك (زيادة المبيعات، الوعي، الزوار)
2. اعرف جمهورك (العمر، الاهتمامات، الموقع)
3. اختر منصة واحدة للبدء
4. صمم إعلان بسيط وواضح
5. ابدأ بميزانية صغيرة (100-200 ج/يوم)
6. راقب النتائج يومياً
7. حسّن باستمرار
` : `
💡 **نصائح عامة بناءً على بياناتك:**
${avgROI < 100 ? "- ⚠️ ROI منخفض، راجع استهداف الجمهور والكلمات المفتاحية" : ""}
${avgROI > 150 ? "- ✅ أداء ممتاز! استثمر أكثر في الحملات الناجحة" : ""}
${activeCampaigns === 0 ? "- ⚠️ لا توجد حملات نشطة، ابدأ حملة جديدة" : ""}
${totalConversions < 10 ? "- 🎯 حسّن معدل التحويل بتحسين صفحة الهبوط" : ""}
`}

❓ **أسئلة يمكنك طرحها:**
- "كيف أزيد ROI؟"
- "كيف أحسن معدل النقر؟"
- "أي منصة أفضل لمنتجاتي؟"
- "كيف أقلل تكلفة التحويل؟"
- "كيف أحسن SEO موقعي؟"

💬 اسألني أي شيء محدد وسأساعدك!`;
}
