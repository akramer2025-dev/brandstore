# 🧪 دليل تجربة نظام الطلبات - Order Testing Guide

## 📋 نظرة عامة
هذا الدليل يشرح كيفية إجراء تجربة كاملة لنظام الطلبات من البداية للنهاية.

---

## 🎯 الحسابات المتاحة للتجربة

بعد تشغيل `npm run prisma:seed`، ستتوفر الحسابات التالية:

### 1️⃣ عميل عادي (Customer)
لم يتم إنشاء حساب عميل افتراضي، لذا يجب تسجيل حساب جديد من الموقع.

### 2️⃣ صاحب محل/بائع (Vendor)
- **البريد الإلكتروني:** `store@partner.com`
- **كلمة المرور:** `Aazxc`
- **الدور:** VENDOR (صاحب محل)
- **اسم المحل:** محل أحمد للأزياء

### 3️⃣ مندوب التوصيل (Delivery Driver)
- **البريد الإلكتروني:** `delivery@partner.com`
- **كلمة المرور:** `Aazxc`
- **الدور:** DELIVERY_STAFF

### 4️⃣ الإدارة (Admin)
- **قم بإنشاء حساب مدير من النظام**
- **الدور:** ADMIN

---

## 🚀 خطوات التجربة الكاملة

### ⚙️ الخطوة 0: التحضير

```bash
# تأكد من تشغيل الخادم
npm run dev

# في نافذة أخرى، افتح Prisma Studio لمتابعة البيانات
npm run prisma:studio
```

افتح متصفحات أو نوافذ incognito متعددة:
- **نافذة 1:** حساب العميل
- **نافذة 2:** حساب البائع
- **نافذة 3:** حساب مندوب التوصيل
- **نافذة 4:** حساب الإدارة

---

### 👤 الخطوة 1: إنشاء حساب عميل وتسجيل الدخول

#### 1.1 إنشاء حساب عميل جديد
1. افتح `http://localhost:3000`
2. اضغط على "تسجيل" أو "Sign Up"
3. املأ البيانات:
   - الاسم: "محمود العميل"
   - البريد: `customer@test.com`
   - اسم المستخدم: `customer1`
   - كلمة المرور: `customer123`
4. سجل الدخول بالحساب الجديد

#### 1.2 استعرض المنتجات
- يجب أن تشاهد منتجات من البائع (صاحب المحل)
- ابحث عن منتجات تابعة للـ Vendor المسجل

---

### 🛒 الخطوة 2: إضافة منتجات للعربة وإنشاء طلب

#### 2.1 إضافة منتجات
1. اختر منتج من منتجات "محل أحمد"
2. اضغط "إضافة للسلة"
3. كرر العملية لمنتج آخر إذا أردت

#### 2.2 عرض السلة
1. اضغط على أيقونة السلة في الهيدر
2. راجع المنتجات والأسعار
3. اضغط "إتمام الطلب" أو "Checkout"

#### 2.3 إتمام الطلب
املأ بيانات التوصيل:
```
الاسم: محمود العميل
رقم الهاتف: 01234567890
العنوان: 15 شارع النيل، المعادي، القاهرة
ملاحظات: الرجاء الاتصال قبل التوصيل
```

اختر طريقة الدفع:
- ✅ الدفع عند الاستلام (Cash on Delivery)
- أو التقسيط إذا كان متاحًا

اضغط **"تأكيد الطلب"**

#### 2.4 احفظ معلومات الطلب
بعد إنشاء الطلب، ستظهر رسالة نجاح مع:
- **رقم الطلب (Order ID):** احفظه!
- **إجمالي المبلغ**
- حالة الطلب: `PENDING` (قيد الانتظار)

📝 **مثال:**
```
Order ID: cm5abc123xyz
Total: 250 ج.م
Status: PENDING
```

---

### 👨‍💼 الخطوة 3: مراجعة الطلب من لوحة الإدارة

#### 3.1 تسجيل الدخول كـ Admin
في نافذة جديدة:
1. اذهب إلى `http://localhost:3000/auth/login`
2. سجل دخول بحساب الإدارة (قم بإنشاء حساب مدير أولاً)

#### 3.2 عرض الطلبات
1. اذهب إلى لوحة التحكم: `http://localhost:3000/admin`
2. اضغط على **"Orders"** أو **"الطلبات"**
3. يجب أن تشاهد الطلب الجديد في القائمة

#### 3.3 معلومات الطلب في لوحة الإدارة
ستشاهد:
- ✅ تفاصيل العميل (الاسم، الهاتف، العنوان)
- ✅ تفاصيل المنتجات والأسعار
- ✅ حالة الطلب: `PENDING`
- ✅ اسم البائع: "محل أحمد"
- ✅ خيارات الإدارة:
  - تأكيد الطلب
  - تعيين مندوب توصيل
  - إلغاء الطلب

#### 3.4 تأكيد الطلب وتعيين مندوب
1. اضغط **"تأكيد الطلب"** → سيتغير الحالة إلى `CONFIRMED`
2. اضغط **"تعيين مندوب توصيل"**
3. اختر "علي المندوب" من القائمة
4. احفظ التعيين

✅ **الآن الطلب أصبح:**
- Status: `CONFIRMED` أو `OUT_FOR_DELIVERY`
- Assigned to: علي المندوب

---

### 🏪 الخطوة 4: مراجعة الطلب من حساب البائع (Vendor)

#### 4.1 تسجيل الدخول كـ Vendor
في نافذة جديدة:
1. اذهب إلى `http://localhost:3000/auth/login`
2. سجل دخول بحساب البائع:
   - البريد: `store@partner.com`
   - كلمة المرور: `Aazxc`

#### 4.2 عرض لوحة البائع
1. اذهب إلى `http://localhost:3000/vendor`
2. اضغط على **"Orders"** أو **"إدارة الطلبات"**
3. يجب أن تشاهد الطلب الذي يحتوي على منتجاتك فقط

#### 4.3 معلومات الطلب في لوحة البائع
ستشاهد:
- ✅ رقم الطلب
- ✅ تفاصيل العميل
- ✅ المنتجات من محلك فقط (لا يرى منتجات بائعين آخرين)
- ✅ حالة الطلب الحالية
- ✅ مندوب التوصيل المعين
- ✅ إجمالي قيمة منتجاتك
- ✅ العمولة المخصومة (15%)
- ✅ صافي الأرباح

**مثال:**
```
إجمالي المنتجات: 250 ج.م
العمولة (15%): -37.5 ج.م
صافي الربح: 212.5 ج.م
```

#### 4.4 خيارات البائع
- تحديث حالة المنتج (جاهز للتسليم)
- التواصل مع الإدارة
- طباعة فاتورة
- عرض تفاصيل الطلب

---

### 🚗 الخطوة 5: مراجعة الطلب من حساب مندوب التوصيل

#### 5.1 تسجيل الدخول كـ Delivery Staff
في نافذة جديدة:
1. اذهب إلى `http://localhost:3000/auth/login`
2. سجل دخول بحساب المندوب:
   - البريد: `delivery@partner.com`
   - كلمة المرور: `Aazxc`

#### 5.2 عرض لوحة التوصيل
1. اذهب إلى `http://localhost:3000/delivery-dashboard`
2. ستشاهد الطلبات المعينة لك

#### 5.3 معلومات الطلب في لوحة المندوب
ستشاهد:
- ✅ رقم الطلب
- ✅ اسم العميل
- ✅ عنوان التوصيل الكامل
- ✅ رقم هاتف العميل
- ✅ إجمالي المبلغ المطلوب تحصيله
- ✅ رسوم التوصيل
- ✅ تفاصيل المنتجات
- ✅ حالة الطلب

#### 5.4 خيارات المندوب
1. **تحديث الحالة:**
   - خرج للتوصيل: `OUT_FOR_DELIVERY`
   - تم التوصيل: `DELIVERED`
   - تم الرفض: `REJECTED`

2. **عند الوصول للعميل:**
   - إذا قبل العميل: `DELIVERED` → تحصيل المبلغ الكامل + رسوم التوصيل
   - إذا رفض العميل: `REJECTED` → تحصيل رسوم التوصيل فقط + إرجاع للمخزون

3. **معلومات إضافية:**
   - خريطة للموقع (إذا متاحة)
   - زر للاتصال بالعميل
   - ملاحظات العميل

---

## 📊 متابعة البيانات في Prisma Studio

افتح Prisma Studio: `http://localhost:5555`

### الجداول المهمة للمراجعة:

#### 1️⃣ جدول Order
- شاهد الطلب الجديد
- تحقق من الحقول:
  - `customerId`: ID العميل
  - `status`: حالة الطلب
  - `deliveryStaffId`: ID مندوب التوصيل
  - `total`: إجمالي المبلغ
  - `deliveryAddress`: عنوان التوصيل

#### 2️⃣ جدول OrderItem
- شاهد منتجات الطلب
- تحقق من:
  - `productId`: ID المنتج
  - `quantity`: الكمية
  - `price`: السعر
  - `vendorId`: ID البائع

#### 3️⃣ جدول Product
- تحقق من تحديث الكمية المتاحة بعد الطلب
- `stock` يجب أن يقل تلقائيًا

#### 4️⃣ جدول Vendor
- شاهد معلومات البائع
- `commissionRate`: نسبة العمولة (15%)

#### 5️⃣ جدول User
- شاهد جميع المستخدمين ودورهم
- `role`: CUSTOMER, VENDOR, ADMIN, DELIVERY_STAFF

---

## 🔄 سيناريوهات اختبار إضافية

### سيناريو 1: رفض العميل للطلب
1. سجل دخول كمندوب
2. حدث حالة الطلب إلى `REJECTED`
3. تحقق من:
   - ✅ إعادة المنتجات للمخزون
   - ✅ تحصيل رسوم التوصيل فقط
   - ✅ ظهور الطلب كمرفوض في جميع اللوحات

### سيناريو 2: طلب من عدة بائعين
1. أضف منتجات من بائعين مختلفين لنفس الطلب
2. تحقق من:
   - كل بائع يرى منتجاته فقط
   - الإدارة ترى الطلب كاملاً
   - توزيع العمولات صحيح

### سيناريو 3: تقسيط
1. اختر خطة تقسيط عند إنشاء الطلب
2. تحقق من:
   - جدول الأقساط في قاعدة البيانات
   - عرض الأقساط في لوحة العميل
   - متابعة المدفوعات

### سيناريو 4: تتبع الطلب
1. كعميل، اذهب إلى `http://localhost:3000/orders`
2. اضغط على الطلب لرؤية التفاصيل
3. تحقق من:
   - Timeline الطلب
   - الحالة الحالية
   - معلومات مندوب التوصيل

---

## 🔍 نقاط التحقق الرئيسية

### ✅ بالنسبة للعميل:
- [ ] إنشاء الطلب بنجاح
- [ ] استلام تأكيد بالطلب
- [ ] القدرة على تتبع الطلب
- [ ] رؤية معلومات مندوب التوصيل
- [ ] استلام إشعارات عند تغيير الحالة

### ✅ بالنسبة للإدارة:
- [ ] رؤية جميع الطلبات
- [ ] تأكيد/إلغاء الطلبات
- [ ] تعيين مندوبي التوصيل
- [ ] متابعة حالات الطلبات
- [ ] رؤية تفاصيل كل طلب كاملة

### ✅ بالنسبة للبائع:
- [ ] رؤية الطلبات التي تحتوي منتجاته فقط
- [ ] معرفة صافي الأرباح بعد العمولة
- [ ] تحديث حالة جاهزية المنتجات
- [ ] رؤية معلومات العميل والتوصيل
- [ ] لا يرى منتجات بائعين آخرين في نفس الطلب

### ✅ بالنسبة لمندوب التوصيل:
- [ ] رؤية الطلبات المعينة له فقط
- [ ] الوصول لمعلومات التواصل مع العميل
- [ ] تحديث حالة التوصيل
- [ ] معرفة المبلغ المطلوب تحصيله
- [ ] القدرة على الإبلاغ عن مشاكل التوصيل

---

## 🐛 حل المشاكل الشائعة

### المشكلة: لا تظهر الطلبات للبائع
**الحل:**
- تأكد أن المنتجات في الطلب لها `vendorId` صحيح
- راجع استعلام SQL في `src/app/vendor/orders/page.tsx`

### المشكلة: لا يستطيع المندوب تحديث الحالة
**الحل:**
- تأكد من تعيين المندوب للطلب من لوحة الإدارة
- تحقق من صلاحيات API في `src/app/api/orders/[id]/route.ts`

### المشكلة: المخزون لا يتحدث تلقائياً
**الحل:**
- راجع `OrderService.createOrder()` في `src/lib/order-service.ts`
- تأكد من تفعيل automatic inventory management

---

## 📸 لقطات شاشة مهمة

### يُنصح بأخذ لقطات شاشة للتالي:
1. ✅ صفحة إنشاء الطلب (Checkout)
2. ✅ تأكيد الطلب
3. ✅ الطلب في لوحة الإدارة
4. ✅ الطلب في لوحة البائع
5. ✅ الطلب في لوحة المندوب
6. ✅ تتبع الطلب من حساب العميل
7. ✅ Prisma Studio - جدول Orders

---

## 📞 الخطوات التالية

بعد التجربة الناجحة:
1. ✅ اختبار جميع حالات الطلب (PENDING, CONFIRMED, etc.)
2. ✅ اختبار سيناريو الرفض والإرجاع
3. ✅ اختبار التقسيط والدفع الإلكتروني
4. ✅ إضافة إشعارات في الوقت الفعلي
5. ✅ إضافة تتبع GPS لمندوب التوصيل
6. ✅ إضافة تقييمات للطلبات المكتملة

---

## 🎉 ملخص رحلة الطلب

```
1. العميل → إنشاء الطلب → PENDING
            ↓
2. الإدارة → تأكيد الطلب → CONFIRMED
            ↓
3. الإدارة → تعيين مندوب → OUT_FOR_DELIVERY
            ↓
4. المندوب → يستلم الطلب → يشاهد التفاصيل
            ↓
5. البائع → يشاهد طلباته → يحضر المنتجات
            ↓
6. المندوب → التوصيل للعميل → تحديث الحالة
            ↓
7. إما DELIVERED (قبول) أو REJECTED (رفض)
```

---

**ملاحظة:** هذا الدليل قابل للتحديث بناءً على التطورات الجديدة في النظام.

**تاريخ الإنشاء:** 9 يناير 2025  
**الإصدار:** 1.0
