# 📍 نظام عنوان المتجر والتقسيط - Store Address & Installment System

## ✅ **تم إضافة النظام بنجاح!**

---

## 🎯 **التحديثات الجديدة:**

### 1️⃣ **عنوان المتجر (Store Address)**
نظام كامل لإدارة عنوان المتجر لاستلام الشحنات من بوسطة

#### **الحقول المضافة في Vendor Model:**
```prisma
governorate        String?   // المحافظة
city               String?   // المدينة/المركز
region             String?   // الحي/المنطقة
district           String?   // المنطقة
street             String?   // اسم الشارع
buildingNumber     String?   // رقم المبنى/العمارة
floorNumber        String?   // رقم الطابق
apartmentNumber    String?   // رقم الشقة
landmark           String?   // أقرب معلم
postalCode         String?   // الرمز البريدي
pickupInstructions String?   // تعليمات استلام الشحنات
```

#### **الصفحة الجديدة:**
📄 `/vendor/store-address` - صفحة إدارة عنوان المتجر

**المزايا:**
- ✅ إضافة عنوان المتجر بالتفصيل
- ✅ التحقق من الحقول المطلوبة (محافظة، مدينة، شارع)
- ✅ توليد العنوان الكامل تلقائياً
- ✅ إضافة تعليمات خاصة بالاستلام
- ✅ واجهة سهلة وواضحة

#### **API Endpoints:**
```typescript
GET  /api/vendor/store-address  // جلب عنوان المتجر
PUT  /api/vendor/store-address  // تحديث عنوان المتجر
```

---

### 2️⃣ **التكامل مع بوسطة (Bosta Integration)**
تحديث نظام الشحن لدعم عنوان استلام وعنوان توصيل

#### **التحديثات في Bosta Service:**
```typescript
interface BostaDeliveryData {
  // Pickup (Vendor Store Address) - عنوان الاستلام من المتجر
  pickupAddress?: string;
  pickupCity?: string;
  pickupGovernorate?: string;
  pickupPhone?: string;
  pickupName?: string;
  pickupInstructions?: string;
  
  // Delivery (Customer Address) - عنوان التوصيل للعميل
  customerName: string;
  customerPhone: string;
  deliveryAddress: string;
  city: string;
  cashOnDelivery: number;
  notes?: string;
}
```

#### **التحقق من العنوان قبل الشحن:**
- ✅ يتحقق من وجود عنوان المتجر
- ✅ يتحقق من وجود المحافظة والمدينة والشارع
- ✅ يعرض رسالة واضحة في حالة نقص البيانات
- ✅ يبني العنوان الكامل تلقائياً

#### **تحديث Ship Endpoint:**
```
POST /api/orders/[id]/ship
```

**الآن يرسل:**
- 📍 عنوان الاستلام من المتجر (Pickup Address)
- 📍 عنوان التوصيل للعميل (Delivery Address)
- 📞 رقم تليفون المتجر + رقم تليفون العميل
- 📝 تعليمات الاستلام (إن وجدت)

---

### 3️⃣ **نظام التقسيط (Installment System)**
عرض خيار التقسيط مع علامة "قريباً"

#### **التحديث في صفحة Checkout:**
```tsx
// Badge "قريباً" على خيار التقسيط
<div className="bg-gradient-to-r from-purple-600 to-pink-600 text-white px-3 py-1 rounded-full text-xs font-bold">
  قريباً ⭐
</div>
```

**المزايا:**
- ✅ يظهر خيار "الدفع بالتقسيط" في صفحة الدفع
- ✅ مكتوب عليه "قريباً" بشكل واضح
- ✅ معطّل مؤقتاً (opacity-60 + cursor-not-allowed)
- ✅ رسالة: "سنوفر قريباً خدمة التقسيط بالتعاون مع شركات التمويل الرائدة"

#### **خيارات التقسيط المتاحة (مستقبلاً):**
- `INSTALLMENT_4` - 4 أشهر
- `INSTALLMENT_6` - 6 أشهر
- `INSTALLMENT_12` - 12 شهر
- `INSTALLMENT_24` - 24 شهر

---

## 🎯 **كيفية الاستخدام:**

### **للشريك (Vendor):**

1. **إضافة عنوان المتجر:**
   - سجل دخول كـ Vendor
   - اذهب إلى Dashboard
   - اضغط على كارد "عنوان المتجر
"
   - أدخل بيانات العنوان بالتفصيل
   - احفظ

2. **شحن طلب:**
   - اذهب للطلب المطلوب شحنه
   - تأكد أن الطلب:
     * الحالة: CONFIRMED أو PREPARING
     * التوصيل: HOME_DELIVERY
     * معاه عنوان توصيل كامل
   - اضغط "إرسال لبوسطة"
   - النظام سيتحقق من عنوان المتجر تلقائياً
   - إذا كان ناقص، سيعرض رسالة واضحة

---

## 🔍 **التحقق من البيانات:**

### **قبل الشحن، النظام يتأكد من:**
✅ الطلب له vendor
✅ الـ vendor معاه عنوان متجر
✅ عنوان المتجر فيه: محافظة + مدينة + شارع
✅ الطلب معاه عنوان توصيل كامل
✅ رقم تليفون العميل موجود

---

## 📊 **الملفات المعدلة:**

### **Database Schema:**
- ✅ `prisma/schema.prisma` - إضافة حقول العنوان في Vendor model

### **API Endpoints:**
- ✅ `src/app/api/vendor/store-address/route.ts` - API جديد لإدارة العنوان
- ✅ `src/app/api/orders/[id]/ship/route.ts` - تحديث لدعم pickup address

### **Frontend Pages:**
- ✅ `src/app/vendor/store-address/page.tsx` - صفحة جديدة لإدارة العنوان
- ✅ `src/app/vendor/dashboard/page.tsx` - إضافة كارد "عنوان المتجر"
- ✅ `src/app/checkout/page.tsx` - إضافة badge "قريباً" للتقسيط

### **Services:**
- ✅ `src/lib/bosta-service.ts` - تحديث لدعم pickup + delivery addresses

---

## 🚀 **الخطوات التالية:**

### **لحين رد بوسطة:**
1. ✅ النظام جاهز بالكامل
2. ✅ عنوان المتجر يمكن إضافته
3. ✅ Shipping endpoint جاهز
4. ⏳ ننتظر رد من بوسطة للتحقق من API Key
5. ⏳ اختبار الشحن الفعلي

### **للتقسيط:**
1. ✅ الخيار يظهر كـ "قريباً"
2. ⏳ التعاقد مع شركات التمويل
3. ⏳ تفعيل النظام لاحقاً

---

## 📞 **أي استفسارات؟**

النظام الآن كامل ومتكامل! 🎉

**المطلوب فقط:**
- التواصل مع بوسطة لتفعيل API Key
- اختبار الشحن الفعلي

---

**✨ نظام احترافي وجاهز للاستخدام! ✨**
