# تحديث نظام إدارة الشركاء

## 📋 ملخص التحديثات

تم نقل نظام إدارة الشركاء من حساب البائع (VENDOR) إلى حساب المدير (ADMIN) فقط.

## ✅ التغييرات المنفذة

### 1. نقل صفحة إدارة الشركاء
- **حُذفت**: `/vendor/partners`
- **أُضيفت**: `/admin/partners`
- **السبب**: الشركاء يُديرهم المدير فقط، وليس البائعين العاديين

### 2. إنشاء API خاص بالمدير
- **الملف**: `/api/admin/partners/route.ts`
- **المميزات**:
  - GET: جلب جميع الشركاء من جميع البائعين
  - POST: إضافة شريك جديد
  - **فحص الصلاحيات**: يتحقق أن المستخدم ADMIN فقط
  - **خيار إنشاء حساب**: يمكن إنشاء حساب VENDOR للشريك تلقائيًا

### 3. تحديث لوحة التحكم للبائع
- **الملف**: `/vendor/dashboard/page.tsx`
- **التغيير**: حذف زر "الشركاء" من الملخص السريع
- **قبل**: 4 عناصر في الملخص (الإيداعات، مملوكة، وسيط، الشركاء)
- **بعد**: 3 عناصر فقط (الإيداعات، مملوكة، وسيط)

### 4. تحديث لوحة التحكم للمدير
- **الملف**: `/admin/page.tsx`
- **التغيير**: إضافة كارت "إدارة الشركاء"
- **الموقع**: بين "العملاء" و "إعدادات الموقع"
- **اللون**: text-emerald-600
- **الأيقونة**: Users

## 🔐 الصلاحيات

### المدير (ADMIN)
- ✅ يمكنه الوصول لـ `/admin/partners`
- ✅ يمكنه إضافة شركاء جدد
- ✅ يمكنه عرض جميع الشركاء من جميع البائعين
- ✅ يمكنه إنشاء حساب VENDOR للشريك

### البائع (VENDOR)
- ❌ لا يمكنه الوصول لإدارة الشركاء
- ❌ تم إزالة الزر من لوحة التحكم
- ❌ تم حذف صفحة `/vendor/partners`

## 📊 سير عمل إضافة شريك

1. المدير يفتح `/admin/partners`
2. يضغط على "إضافة شريك جديد"
3. يملأ البيانات:
   - اسم الشريك
   - البريد الإلكتروني
   - رقم الهاتف (اختياري)
   - مبلغ رأس المال
   - نسبة الشراكة (%)
   - نوع الشريك (شريك، مستثمر، صامت)
   - ملاحظات (اختياري)
   - **خيار**: إنشاء حساب VENDOR للشريك؟
4. عند الحفظ:
   - يتم إنشاء سجل في `PartnerCapital`
   - يتم تحديث `capitalBalance` للـ vendor
   - يتم إنشاء معاملة `DEPOSIT` في `CapitalTransaction`
   - **إذا تم اختيار إنشاء حساب**:
     - يتم إنشاء User بدور VENDOR
     - يتم إنشاء Vendor مرتبط بالمستخدم
     - يتم توليد كلمة مرور عشوائية (TODO: إرسالها بالبريد)

## 🧪 الاختبار

### تسجيل الدخول كمدير
```
البريد: akram@gmail.com
كلمة المرور: Aazxc
الدور: ADMIN
```

### اختبار الوصول
1. سجل دخول كمدير
2. افتح `/admin/partners`
3. جرب إضافة شريك جديد
4. تحقق من ظهور الشريك في القائمة
5. تحقق من تحديث رأس المال

### اختبار البائع
1. سجل دخول كبائع عادي
2. افتح `/vendor/dashboard`
3. تأكد من عدم وجود زر "الشركاء"
4. حاول الوصول لـ `/vendor/partners` (يجب أن يُعيد توجيهك أو يظهر 404)

## 📁 الملفات المحدثة

```
src/
├── app/
│   ├── admin/
│   │   ├── page.tsx                    ✅ محدث (إضافة كارت الشركاء)
│   │   └── partners/
│   │       └── page.tsx                ✅ جديد (صفحة إدارة الشركاء)
│   ├── api/
│   │   └── admin/
│   │       └── partners/
│   │           └── route.ts            ✅ جديد (API للمدير)
│   └── vendor/
│       ├── dashboard/
│       │   └── page.tsx                ✅ محدث (إزالة زر الشركاء)
│       └── partners/                   ❌ محذوف
```

## 🔄 الخطوات التالية (اختياري)

1. **إرسال البريد الإلكتروني**
   - عند إنشاء حساب للشريك، يجب إرسال بريد إلكتروني بكلمة المرور

2. **تعديل بيانات الشريك**
   - إضافة endpoint PUT لتحديث بيانات الشريك
   - إضافة نموذج تعديل في الواجهة

3. **حذف شريك**
   - إضافة endpoint DELETE
   - التعامل مع رأس المال المرتبط

4. **تقارير الشركاء**
   - إضافة تقرير بالأرباح لكل شريك
   - حساب العوائد بناءً على النسبة

## 🎯 الهدف المحقق

✅ **فصل صلاحيات الشركاء**:
- المدير (ADMIN) = يدير النظام والشركاء
- البائع (VENDOR) = يدير منتجاته وطلباته فقط
- الشريك = له حساب VENDOR إذا أراد، أو مجرد سجل رأس مال

---

**التاريخ**: 2025-02-01
**الحالة**: ✅ مكتمل وجاهز للاختبار
**السيرفر**: 🟢 يعمل على http://localhost:3000
