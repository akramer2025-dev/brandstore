# دليل حماية البيانات والتغييرات

## 🔒 كيف تحمي بياناتك من الحذف؟

### 1. **لا تستخدم Reset أبداً بعد اليوم!**
❌ **خطير - يحذف كل شيء:**
```bash
npx prisma migrate reset
npx prisma db push --force-reset
```

✅ **آمن - يحفظ البيانات:**
```bash
npx prisma migrate dev --name add_feature_name
npx prisma db push
```

---

### 2. **عمل نسخة احتياطية يومية**

**في Windows:**
```bash
.\backup-database.bat
```

**في Linux/Mac:**
```bash
npm run backup
```

**تلقائياً كل يوم:** 
ضع الملف `backup-database.bat` في Task Scheduler على Windows

---

### 3. **استخدام Git لحفظ الكود**

```bash
# أول مرة فقط
git init
git add .
git commit -m "Initial commit"

# كل مرة تعمل تعديل:
git add .
git commit -m "Add e-wallet payment feature"

# إذا عملت حاجة غلط، استرجع النسخة القديمة:
git log  # شوف قائمة التغييرات
git checkout [commit-id]  # ارجع لنقطة معينة
```

---

### 4. **رفع المشروع على GitHub (مهم جداً!)**

```bash
# أنشئ repo على GitHub أولاً، ثم:
git remote add origin https://github.com/yourusername/your-repo.git
git branch -M main
git push -u origin main

# كل مرة تعمل تحديث:
git add .
git commit -m "Add new feature"
git push
```

---

### 5. **حماية ملف .env**

**ملف `.gitignore`** (موجود فعلاً في المشروع):
```
.env
.env.local
node_modules/
.next/
```

✅ **كده ملف .env مش هيتحمل على GitHub** (حفاظاً على أسرار قاعدة البيانات)

---

## 📊 قاعدة البيانات الحالية

### البيانات الموجودة حالياً:
- ✅ 3 مستخدمين
- ✅ 4 فئات
- ✅ 20 منتج

### بيانات الدخول:
```
المدير:  admin@bs.com / 123456
البائع:  vendor@bs.com / 123456
العميل:  customer@bs.com / 123456
```

---

## 🎯 خطة الحماية المقترحة

### يومياً:
1. ✅ عمل commit على Git بعد أي تعديل
2. ✅ عمل push على GitHub

### أسبوعياً:
1. ✅ نسخة احتياطية لقاعدة البيانات
2. ✅ مراجعة التغييرات

### شهرياً:
1. ✅ نسخة احتياطية كاملة للمشروع
2. ✅ تحديث الـ dependencies

---

## ⚠️ تحذيرات مهمة

### ❌ لا تفعل هذا أبداً:
- `npx prisma migrate reset` ❌
- `npx prisma db push --force-reset` ❌
- حذف مجلد `prisma/migrations` ❌
- تعديل ملفات migration يدوياً ❌

### ✅ افعل هذا دائماً:
- `npx prisma migrate dev --name feature_name` ✅
- `git commit` بعد كل تعديل ✅
- نسخة احتياطية قبل أي تغيير كبير ✅
- اختبار التعديلات قبل التطبيق ✅

---

## 🚀 المشروع جاهز الآن!

كل التعديلات اللي عملناها:
- ✅ طريقة دفع المحفظة الإلكترونية
- ✅ تقليل ارتفاع الـ Header
- ✅ استعادة البيانات

كل حاجة محفوظة ومش هتتمسح تاني إلا لو استخدمت الأوامر الخطيرة! 🎉
