# دليل حماية البيانات والتغييرات

## ⚠️ ما الذي حدث للطلب؟

### السبب: استخدام أوامر الحذف الخطيرة!

الأوامر التي استخدمتها:
```bash
npx prisma db push --force-reset  # ❌ هذا الأمر حذف كل البيانات!
npx tsx seed-simple.ts            # ثم أعاد seed بيانات جديدة
```

**لهذا السبب:**
- ✅ المستخدمين موجودين (من seed)
- ✅ المنتجات موجودة (من seed)
- ❌ الطلب الذي عملته **اتمسح**!

---

## 🔒 كيف تحمي بياناتك من الحذف؟

### 1. **لا تستخدم Reset أبداً بعد اليوم!**
❌ **خطير - يحذف كل شيء:**
```bash
npx prisma migrate reset
npx prisma db push --force-reset
```

✅ **آمن - يحفظ البيانات:**
```bash
npx prisma migrate dev --name add_feature_name
npx prisma db push
```

---

### 2. **قواعد حذف البيانات (متى يُسمح بالحذف)**

النظام الآن محمي - البيانات **لا تُحذف أبداً** إلا في هذه الحالات:

#### ✅ حالات الحذف المسموح بها:

**1. المنتجات:**
- التاجر يحذف منتجه (Soft Delete - المنتج يختفي لكن بياناته موجودة)
- المطور يحذف منتج (في حالات استثنائية)

**2. الطلبات:**
- العميل يطلب إلغاء الطلب قبل التوصيل
- المطور يلغي طلب (بسبب مشكلة)
- **الطلب نفسه لا يُحذف** - فقط الحالة تتغير لـ CANCELLED

**3. التاجر:**
- المطور يوقف التاجر (Soft Delete - الحساب موجود لكن معطل)
- التاجر لا يقدر يحذف حسابه نفسه

#### ❌ حالات الحذف الممنوعة:
- حذف المستخدمين
- حذف الطلبات القديمة
- حذف سجل المعاملات
- حذف الفواتير

---

### 3. **عمل نسخة احتياطية يومية**

**في Windows:**
```bash
.\backup-database.bat
```

**في Linux/Mac:**
```bash
npm run backup
```

**تلقائياً كل يوم:** 
ضع الملف `backup-database.bat` في Task Scheduler على Windows

---

### 4. **استخدام Git لحفظ الكود**

```bash
# أول مرة فقط
git init
git add .
git commit -m "Initial commit"

# كل مرة تعمل تعديل:
git add .
git commit -m "Add e-wallet payment feature"

# إذا عملت حاجة غلط، استرجع النسخة القديمة:
git log  # شوف قائمة التغييرات
git checkout [commit-id]  # ارجع لنقطة معينة
```

---

### 5. **رفع المشروع على GitHub (مهم جداً!)**

```bash
# أنشئ repo على GitHub أولاً، ثم:
git remote add origin https://github.com/yourusername/your-repo.git
git branch -M main
git push -u origin main

# كل مرة تعمل تحديث:
git add .
git commit -m "Add new feature"
git push
```

---

### 6. **حماية ملف .env**

**ملف `.gitignore`** (موجود فعلاً في المشروع):
```
.env
.env.local
node_modules/
.next/
```

✅ **كده ملف .env مش هيتحمل على GitHub** (حفاظاً على أسرار قاعدة البيانات)

---

## 📊 قاعدة البيانات الحالية

### البيانات الموجودة حالياً:
- ✅ 3 مستخدمين
- ✅ 4 فئات
- ✅ 20 منتج

### بيانات الدخول:
```
المدير:  admin@bs.com / 123456
البائع:  vendor@bs.com / 123456
العميل:  customer@bs.com / 123456
```

---

## 🛡️ نظام الحماية الجديد (Soft Delete)

### ما هو Soft Delete؟
بدلاً من حذف البيانات نهائياً، نقوم بـ "تعليمها كمحذوفة":
- يضاف حقل `deletedAt` (تاريخ الحذف)
- يضاف حقل `deletedBy` (من قام بالحذف)
- يضاف حقل `deletedReason` (سبب الحذف)

### مميزات Soft Delete:
✅ **الحفاظ على السجلات**: البيانات موجودة دائماً في قاعدة البيانات
✅ **إمكانية الاسترجاع**: يمكن استرجاع أي بيانات محذوفة
✅ **تدقيق كامل**: معرفة من ومتى ولماذا تم الحذف
✅ **حماية من الأخطاء**: لا يمكن حذف البيانات بالخطأ نهائياً

### الجداول المحمية:
1. **Orders** (الطلبات) ✅ - مطبق الآن
2. **Products** (المنتجات) ✅ - موجود بالفعل (حقل `isActive`)
3. **Users** (المستخدمين) - محمي بعدم السماح بالحذف
4. **Vendors** (التجار) - محمي بعدم السماح بالحذف

### كيف تعمل الطلبات الآن:
```sql
-- الطلبات الموجودة فقط (deletedAt = null)
SELECT * FROM orders WHERE deletedAt IS NULL;

-- الطلبات المحذوفة فقط
SELECT * FROM orders WHERE deletedAt IS NOT NULL;

-- جميع الطلبات (بما فيها المحذوفة)
SELECT * FROM orders;
```

### APIs الجديدة:
```
DELETE /api/admin/orders/:id/delete
- يحذف الطلب (Soft Delete)
- يتطلب صلاحية ADMIN
- يسجل سبب الحذف

POST /api/admin/orders/:id/delete
- يسترجع طلب محذوف
- يتطلب صلاحية ADMIN
```

---

## 🔐 قواعد الحماية النهائية

### 1. الأوامر الآمنة فقط
✅ **استخدم:**
```bash
npx prisma db push              # تطبيق التغييرات بأمان
npx prisma migrate dev          # إنشاء migration جديد
npx prisma studio               # تصفح البيانات
```

❌ **لا تستخدم أبداً:**
```bash
npx prisma migrate reset        # يحذف كل شيء!
npx prisma db push --force-reset # يحذف كل شيء!
```

### 2. النسخ الاحتياطي التلقائي
- اعمل backup يومي على الأقل
- احفظ النسخ على Google Drive أو خدمة سحابية

### 3. Git للكود، Backup لقاعدة البيانات
- Git: للكود والملفات
- Backup: لقاعدة البيانات فقط

### 4. عدم الحذف = الحماية الكاملة
القاعدة الذهبية: **البيانات لا تُحذف، بل تُخفى**
- الطلبات: Soft Delete
- المنتجات: isActive = false
- المستخدمين: لا يُحذفون أبداً
- السجلات المالية: لا تُحذف أبداً

---

## 📝 ملخص التحديثات

### ما تم تطبيقه:
✅ إضافة Soft Delete للطلبات (deletedAt, deletedBy, deletedReason)
✅ تحديث جميع Queries لاستبعاد الطلبات المحذوفة
✅ API لحذف واسترجاع الطلبات (Admin فقط)
✅ حماية البيانات من الحذف غير المقصود

### ما لم يتغير:
✅ جميع الصفحات تعمل كما هي
✅ جميع الوظائف السابقة سليمة
✅ لا توجد تغييرات في الواجهة

---

**تم التحديث:** 4 فبراير 2026  
**الإصدار:** v1.1 - نظام الحماية الكامل

---

## 🎯 خطة الحماية المقترحة

### يومياً:
1. ✅ عمل commit على Git بعد أي تعديل
2. ✅ عمل push على GitHub

### أسبوعياً:
1. ✅ نسخة احتياطية لقاعدة البيانات
2. ✅ مراجعة التغييرات

### شهرياً:
1. ✅ نسخة احتياطية كاملة للمشروع
2. ✅ تحديث الـ dependencies

---

## ⚠️ تحذيرات مهمة

### ❌ لا تفعل هذا أبداً:
- `npx prisma migrate reset` ❌
- `npx prisma db push --force-reset` ❌
- حذف مجلد `prisma/migrations` ❌
- تعديل ملفات migration يدوياً ❌

### ✅ افعل هذا دائماً:
- `npx prisma migrate dev --name feature_name` ✅
- `git commit` بعد كل تعديل ✅
- نسخة احتياطية قبل أي تغيير كبير ✅
- اختبار التعديلات قبل التطبيق ✅

---

## 🚀 المشروع جاهز الآن!

كل التعديلات اللي عملناها:
- ✅ طريقة دفع المحفظة الإلكترونية
- ✅ تقليل ارتفاع الـ Header
- ✅ استعادة البيانات

كل حاجة محفوظة ومش هتتمسح تاني إلا لو استخدمت الأوامر الخطيرة! 🎉
