# دليل توليد ورفع الصور تلقائياً 🎨

## ما الذي يفعله السكريبت؟

السكريبت `generate-product-images.ts` يقوم بـ:

1. ✅ قراءة المنتجات من قاعدة البيانات
2. ✅ تحديد نوع كل منتج (تيشرت، هودي، إلخ)
3. ✅ استخراج اللون من اسم المنتج
4. ✅ توليد صورة احترافية بالذكاء الاصطناعي
5. ✅ تنزيل الصورة وحفظها في `public/uploads`
6. ✅ تحديث قاعدة البيانات بمسار الصورة

---

## الطريقة 1: مع OpenAI API (احترافي) 🤖

### الخطوات:

#### 1. احصل على API Key من OpenAI:
```
1. اذهب إلى: https://platform.openai.com/api-keys
2. سجل دخول أو أنشئ حساب
3. اضغط "Create new secret key"
4. انسخ المفتاح
```

#### 2. أضف المفتاح للـ .env:
```env
# في ملف .env
OPENAI_API_KEY="sk-proj-your-api-key-here"
```

#### 3. شغّل السكريبت:
```bash
npx ts-node generate-product-images.ts
```

### التكلفة:
- **DALL-E 3**: $0.040 لكل صورة (1024x1024)
- **مثال**: 10 منتجات = $0.40 فقط
- **50 منتج** = $2 تقريباً

---

## الطريقة 2: بدون OpenAI (مجاني) 🆓

إذا لم تضف `OPENAI_API_KEY`، السكريبت سيستخدم:
- صور احترافية من **Unsplash**
- مجانية تماماً
- جودة عالية
- متنوعة

```bash
# شغّل مباشرة بدون API Key
npx ts-node generate-product-images.ts
```

---

## كيف يعمل السكريبت؟

### 1. تحديد نوع المنتج:
```typescript
// من اسم المنتج
"تيشرت قطني أبيض" → نوع: تيشرت، لون: white
"هودي رياضي أزرق" → نوع: هودي، لون: navy blue
"طقم رياضي رمادي" → نوع: طقم، لون: heather gray
```

### 2. إنشاء Prompt ذكي:
```
Professional product photography of premium cotton t-shirt 
with small embroidered colorful phoenix brand logo on left chest, 
white, hanging on wooden hanger, clean boutique background, 
soft studio lighting, fashion catalog style, high detail, 4k quality
```

### 3. توليد ورفع:
```
1. يولد الصورة بـ DALL-E
2. ينزلها في public/uploads/product-123-1234567890.jpg
3. يحدث قاعدة البيانات: images = "/uploads/product-123-..."
```

---

## التخصيص:

### تغيير عدد المنتجات:
```typescript
// في generate-product-images.ts السطر 168
const products = await prisma.product.findMany({
  take: 50, // غيّر الرقم هنا (10 → 50)
  orderBy: { createdAt: 'desc' }
});
```

### إضافة أنواع منتجات جديدة:
```typescript
// في السطر 8-30
const productPrompts: Record<string, string> = {
  // أضف نوع جديد:
  'كارديجان': 'Professional product photography of premium cardigan with embroidered brand logo...',
};
```

### إضافة ألوان جديدة:
```typescript
// في السطر 34-46
const colors: Record<string, string> = {
  'أصفر': 'yellow',
  'برتقالي': 'orange',
  // أضف المزيد...
};
```

---

## أمثلة النتائج:

### قبل:
```
❌ المنتجات بصور Unsplash عشوائية
❌ بدون شعار
❌ غير متناسقة
```

### بعد:
```
✅ صور احترافية موحدة
✅ شعار براند ستور على كل قطعة
✅ جودة عالية 1024x1024
✅ ألوان وأنواع دقيقة
```

---

## استكشاف الأخطاء:

### خطأ: "OPENAI_API_KEY غير موجود"
```bash
# حل:
# 1. أضف المفتاح في .env
# 2. أو اتركه فارغ للحصول على صور Unsplash مجانية
```

### خطأ: "Failed to download image"
```bash
# حل:
# تأكد من اتصال الإنترنت
# تأكد من صلاحيات الكتابة في مجلد public/uploads
```

### خطأ: "Rate limit exceeded"
```bash
# حل:
# انتظر دقيقة
# أو قلل عدد المنتجات في المرة الواحدة
```

---

## نصائح للنتائج الأفضل:

### 1. أسماء منتجات واضحة:
```
✅ "تيشرت قطني أبيض سادة"
✅ "هودي رياضي أزرق داكن"
❌ "منتج 1"
❌ "ملابس"
```

### 2. استخدم الألوان المدعومة:
```
أبيض، أسود، أزرق، رمادي، بيج، 
وردي، أخضر، بني، أحمر، تركواز
```

### 3. شغّل على دفعات:
```bash
# الدفعة الأولى: 10 منتجات
npx ts-node generate-product-images.ts

# شوف النتيجة، ثم غيّر take: 50 وشغّل مرة تانية
```

---

## Workflow الموصى به:

### اليوم (التجربة):
```bash
1. شغّل السكريبت على 10 منتجات
2. افتح المتجر: http://localhost:3000
3. شوف النتائج
4. اضبط الـ prompts إذا لزم الأمر
```

### الأسبوع القادم (الإنتاج):
```bash
1. غيّر take إلى 50 أو 100
2. شغّل السكريبت
3. انتظر 5-10 دقائق
4. استمتع بالمتجر الاحترافي! 🎉
```

---

## البدائل:

### إذا لم يعجبك التوليد التلقائي:

#### 1. استخدم Printful Mockup Generator:
```
https://www.printful.com/mockup-generator
- مجاني
- احترافي
- سريع
```

#### 2. استخدم Placeit:
```
https://placeit.net
- $14.95/شهر
- آلاف القوالب
- جودة عالية جداً
```

#### 3. Canva Pro:
```
https://canva.com
- $12.95/شهر
- سهل الاستخدام
- مكتبة ضخمة
```

---

## الأسئلة الشائعة:

### س: كم يستغرق توليد صورة واحدة؟
**ج:** 3-5 ثواني مع OpenAI، فوري مع Unsplash

### س: هل الصور حصرية؟
**ج:** نعم مع DALL-E، لا مع Unsplash (عامة)

### س: يمكن أضيف شعاري الحقيقي على الصور؟
**ج:** نعم! بعد التوليد، استخدم Canva لإضافة شعارك الحقيقي

### س: الصور مش عاجباني، إيه الحل؟
**ج:** 
1. عدّل الـ prompts في السكريبت
2. أو استخدم Photoshop لتحسينها
3. أو استخدم mockup generators بدلاً من ذلك

---

**🚀 جاهز؟ شغّل الأمر:**
```bash
npx ts-node generate-product-images.ts
```

**ولو عندك API Key:**
```bash
# أضف في .env أولاً
OPENAI_API_KEY="sk-proj-..."

# ثم شغّل
npx ts-node generate-product-images.ts
```
