# ✅ كيفية تفعيل خاصية حفظ العناوين وGoogle OAuth

## 📍 نظام حفظ العناوين

### ✨ الميزة شغالة بالفعل!

نظام حفظ العناوين **جاهز ويعمل 100%** في التطبيق! 🎉

#### كيف يعمل؟

1. **في صفحة Checkout:**
   - صندوق اختيار "💾 حفظ هذا العنوان لاستخدامه لاحقاً"
   - اكتب عنوان واضغط على "حفظ العنوان"
   - العنوان يُحفظ تلقائياً بعد إتمام الطلب

2. **في المرة القادمة:**
   - صفحة Checkout تعرض عناوينك المحفوظة تلقائياً
   - اختر عنوان واضغط "إتمام الطلب"
   - توفير الوقت - بدون كتابة العنوان من جديد! ⚡

3. **صفحة إدارة العناوين الجديدة:**
   - افتح: `http://localhost:3000/profile/addresses`
   - أو من القائمة: الملف الشخصي → عناويني
   - **الميزات:**
     - ✅ عرض جميع العناوين
     - ✅ إضافة عنوان جديد
     - ✅ تعديل عنوان موجود
     - ✅ حذف عنوان
     - ✅ تعيين عنوان افتراضي (يُستخدم تلقائياً في الطلبات)

---

## 🔐 تفعيل Google OAuth

### الخطوات السريعة:

#### 1️⃣ احصل على Google Credentials

1. افتح: [Google Cloud Console](https://console.cloud.google.com/)
2. إنشاء مشروع → اسمه: **Remostore**
3. APIs & Services → Credentials
4. Create Credentials → OAuth 2.0 Client ID
5. لو أول مرة، اعمل **Configure Consent Screen**:
   - External
   - App name: Remostore
   - Your email
   - Save
6. Application type: **Web application**
7. **Authorized redirect URIs:**
   ```
   http://localhost:3000/api/auth/callback/google
   http://192.168.3.17:3000/api/auth/callback/google
   https://yourdomain.com/api/auth/callback/google
   ```
8. انسخ **Client ID** و **Client Secret**

#### 2️⃣ حدث ملف .env

افتح `d:\markting\.env` وحط:

```env
# Google OAuth
GOOGLE_CLIENT_ID=<YOUR_CLIENT_ID_HERE>
GOOGLE_CLIENT_SECRET=<YOUR_CLIENT_SECRET_HERE>
```

**مثال:**
```env
GOOGLE_CLIENT_ID=123456789-abc.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-abc123xyz789
```

#### 3️⃣ فعّل Google OAuth في الكود

**في `src/lib/auth.ts`:**

ابحث عن السطر:
```typescript
// ⚠️ Google OAuth معطل مؤقتاً - يحتاج credentials صحيحة
```

شيل التعليق وحط:
```typescript
GoogleProvider({
  clientId: process.env.GOOGLE_CLIENT_ID!,
  clientSecret: process.env.GOOGLE_CLIENT_SECRET!,
}),
```

**في `src/app/auth/login/page.tsx`:**

ابحث عن السطر:
```tsx
{/* زر تسجيل الدخول بـ Google - معطل مؤقتاً */}
```

شيل التعليق وحط:
```tsx
<Button
  type="button"
  onClick={handleGoogleSignIn}
  className="w-full bg-white text-gray-900 hover:bg-gray-100 border border-gray-300"
>
  <svg className="w-5 h-5 mr-2" viewBox="0 0 24 24">...</svg>
  تسجيل الدخول بواسطة Google
</Button>
```

#### 4️⃣ أعد تشغيل السيرفر

```bash
# إغلاق السيرفر الحالي
# اضغط Ctrl+C

# إعادة التشغيل
npm run dev
```

#### 5️⃣ اختبر Google OAuth

1. افتح: `http://localhost:3000/auth/login`
2. اضغط **"تسجيل الدخول بواسطة Google"**
3. اختر حساب Google
4. اسمح بالأذونات
5. ✅ تم تسجيل الدخول!

---

## 🎯 ملخص سريع

### نظام حفظ العناوين:
- ✅ **جاهز ويعمل!** - لا يحتاج أي تعديل
- ✅ صفحة إدارة العناوين: `/profile/addresses`
- ✅ رابط في القائمة: الملف الشخصي → عناويني

### Google OAuth:
- ⏳ **يحتاج credentials من Google Cloud Console**
- ⏳ خطوات سهلة (5 دقائق فقط)
- ⏳ بعد التفعيل، شيل التعليقات من الكود
- ⏳ أعد تشغيل السيرفر

---

## 📚 روابط مفيدة

- [Google Cloud Console](https://console.cloud.google.com/)
- [دليل Google OAuth الكامل](./GOOGLE_OAUTH_SETUP.md)
- [NextAuth Documentation](https://next-auth.js.org/)

---

## 🆘 مشاكل شائعة

### "redirect_uri_mismatch"
**الحل:** تأكد من الـ redirect URI في Google Console مطابق للكود بالظبط

### "Access blocked"
**الحل:** أضف نفسك كـ Test user في OAuth consent screen

### "العناوين لا تظهر"
**الحل:** تأكد من تسجيل الدخول وإنشاء طلب مع "حفظ العنوان" ✓

---

✨ **جاهز للاستخدام!** ✨
